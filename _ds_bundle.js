/* @ds-bundle: {"format":4,"namespace":"AADesignSystem_ada6b1","components":[{"name":"Avatar","sourcePath":"components/acoes/Avatar.jsx"},{"name":"Botao","sourcePath":"components/acoes/Botao.jsx"},{"name":"Dica","sourcePath":"components/acoes/Dica.jsx"},{"name":"Etiqueta","sourcePath":"components/acoes/Etiqueta.jsx"},{"name":"Esqueleto","sourcePath":"components/dados/Esqueleto.jsx"},{"name":"Metrica","sourcePath":"components/dados/Metrica.jsx"},{"name":"Paginacao","sourcePath":"components/dados/Paginacao.jsx"},{"name":"Progresso","sourcePath":"components/dados/Progresso.jsx"},{"name":"Tabela","sourcePath":"components/dados/Tabela.jsx"},{"name":"Vazio","sourcePath":"components/dados/Vazio.jsx"},{"name":"Abas","sourcePath":"components/estrutura/Abas.jsx"},{"name":"Cartao","sourcePath":"components/estrutura/Cartao.jsx"},{"name":"Divisor","sourcePath":"components/estrutura/Divisor.jsx"},{"name":"Gaveta","sourcePath":"components/estrutura/Gaveta.jsx"},{"name":"Migalhas","sourcePath":"components/estrutura/Migalhas.jsx"},{"name":"Alerta","sourcePath":"components/feedback/Alerta.jsx"},{"name":"Aviso","sourcePath":"components/feedback/Aviso.jsx"},{"name":"MenuSuspenso","sourcePath":"components/feedback/MenuSuspenso.jsx"},{"name":"Modal","sourcePath":"components/feedback/Modal.jsx"},{"name":"PilhaAvisos","sourcePath":"components/feedback/PilhaAvisos.jsx"},{"name":"Avisos","sourcePath":"components/feedback/PilhaAvisos.jsx"},{"name":"AreaTexto","sourcePath":"components/formulario/AreaTexto.jsx"},{"name":"Busca","sourcePath":"components/formulario/Busca.jsx"},{"name":"Calendario","sourcePath":"components/formulario/Calendario.jsx"},{"name":"Campo","sourcePath":"components/formulario/Campo.jsx"},{"name":"Escolha","sourcePath":"components/formulario/Escolha.jsx"},{"name":"Interruptor","sourcePath":"components/formulario/Interruptor.jsx"},{"name":"MultiSelecao","sourcePath":"components/formulario/MultiSelecao.jsx"},{"name":"Selecao","sourcePath":"components/formulario/Selecao.jsx"},{"name":"ATALHOS","sourcePath":"components/formulario/SeletorPeriodo.jsx"},{"name":"SeletorPeriodo","sourcePath":"components/formulario/SeletorPeriodo.jsx"},{"name":"Periodo","sourcePath":"components/formulario/SeletorPeriodo.jsx"},{"name":"GraficoBarras","sourcePath":"components/graficos/GraficoBarras.jsx"},{"name":"GraficoDispersao","sourcePath":"components/graficos/GraficoDispersao.jsx"},{"name":"GraficoFunil","sourcePath":"components/graficos/GraficoFunil.jsx"},{"name":"GraficoLinhas","sourcePath":"components/graficos/GraficoLinhas.jsx"},{"name":"GraficoMedidor","sourcePath":"components/graficos/GraficoMedidor.jsx"},{"name":"GraficoRosca","sourcePath":"components/graficos/GraficoRosca.jsx"},{"name":"MapaDeCalor","sourcePath":"components/graficos/MapaDeCalor.jsx"},{"name":"Minigrafico","sourcePath":"components/graficos/Minigrafico.jsx"},{"name":"BarraLateral","sourcePath":"components/navegacao/BarraLateral.jsx"},{"name":"BarraSuperior","sourcePath":"components/navegacao/BarraSuperior.jsx"}],"sourceHashes":{"components/acoes/Avatar.jsx":"a7a7a484ed94","components/acoes/Botao.jsx":"d6138295f63a","components/acoes/Dica.jsx":"4e2c3183c8ed","components/acoes/Etiqueta.jsx":"d2e2ad40a915","components/dados/Esqueleto.jsx":"b7152a5824dd","components/dados/Metrica.jsx":"b5c6db4dc158","components/dados/Paginacao.jsx":"a415d3e03671","components/dados/Progresso.jsx":"33b117e8cd33","components/dados/Tabela.jsx":"5ad1f54a267d","components/dados/Vazio.jsx":"efc60ab3dc11","components/estrutura/Abas.jsx":"d67c87460e2b","components/estrutura/Cartao.jsx":"83062865f017","components/estrutura/Divisor.jsx":"79751a067029","components/estrutura/Gaveta.jsx":"1cf81d6d75c1","components/estrutura/Migalhas.jsx":"08f959176431","components/feedback/Alerta.jsx":"17a72656e8b1","components/feedback/Aviso.jsx":"d1b2860e8692","components/feedback/MenuSuspenso.jsx":"1b8a24a85724","components/feedback/Modal.jsx":"2f5dee611132","components/feedback/PilhaAvisos.jsx":"33ea6caea048","components/formulario/AreaTexto.jsx":"7bb908367aae","components/formulario/Busca.jsx":"cd0c3e5c1b20","components/formulario/Calendario.jsx":"08fade00b25a","components/formulario/Campo.jsx":"6b35cc568813","components/formulario/Escolha.jsx":"acbb9b8abfe5","components/formulario/Interruptor.jsx":"74b12440fccd","components/formulario/MultiSelecao.jsx":"7574ea1e8860","components/formulario/Selecao.jsx":"03a7b91785aa","components/formulario/SeletorPeriodo.jsx":"9b6a483e54df","components/graficos/GraficoBarras.jsx":"40310b3d85dd","components/graficos/GraficoDispersao.jsx":"415ccfba7981","components/graficos/GraficoFunil.jsx":"a3d3aa8e40b9","components/graficos/GraficoLinhas.jsx":"ea73f98a7580","components/graficos/GraficoMedidor.jsx":"ee5ee830fa10","components/graficos/GraficoRosca.jsx":"38c857499972","components/graficos/MapaDeCalor.jsx":"6262b7d2cd7a","components/graficos/Minigrafico.jsx":"8c04a8f08ae2","components/navegacao/BarraLateral.jsx":"1d1eb7f7cfaa","components/navegacao/BarraSuperior.jsx":"537a85194f9d","ui_kits/casa-projetos/App.jsx":"04d59691b641","ui_kits/casa-projetos/MetricCard.jsx":"6953d605b0ea","ui_kits/casa-projetos/NewProjectModal.jsx":"a229c5f0328f","ui_kits/casa-projetos/ProjectDetail.jsx":"4909f7ae3bb4","ui_kits/casa-projetos/ProjectRow.jsx":"87482463c38b","ui_kits/casa-projetos/Sidebar.jsx":"7b78f6b2244a","ui_kits/casa-projetos/TopBar.jsx":"cc1b16f371e9","ui_kits/casa-projetos/primitives.jsx":"443637181e98"},"inlinedExternals":[],"unexposedExports":[{"name":"periodoAnterior","sourcePath":"components/formulario/SeletorPeriodo.jsx"},{"name":"periodoDoAtalho","sourcePath":"components/formulario/SeletorPeriodo.jsx"}]} */

(() => {

const __ds_ns = (window.AADesignSystem_ada6b1 = window.AADesignSystem_ada6b1 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/acoes/Avatar.jsx
try { (() => {
/* global React */
function iniciais(nome) {
  const p = String(nome || '').trim().split(/\s+/);
  return ((p[0] || '')[0] || '').concat(p.length > 1 ? p[p.length - 1][0] || '' : '').toUpperCase();
}
function Avatar({
  nome,
  src,
  tamanho = 32,
  style
}) {
  const base = {
    width: tamanho,
    height: tamanho,
    borderRadius: 999,
    flex: 'none',
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'center',
    fontFamily: 'var(--fonte-ui)',
    fontWeight: 600,
    fontSize: Math.round(tamanho * 0.38),
    background: 'var(--cor-acao-suave-2)',
    color: 'var(--cor-acao-ativa)',
    border: '1px solid var(--cor-borda)',
    overflow: 'hidden',
    ...style
  };
  if (src) return React.createElement('img', {
    src,
    alt: nome || '',
    style: {
      ...base,
      objectFit: 'cover'
    }
  });
  return React.createElement('span', {
    style: base,
    title: nome
  }, iniciais(nome));
}
Object.assign(__ds_scope, { Avatar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/acoes/Avatar.jsx", error: String((e && e.message) || e) }); }

// components/acoes/Botao.jsx
try { (() => {
/* global React */
const {
  useState
} = React;
const VARIANTES = {
  primario: {
    background: 'var(--cor-acao)',
    color: 'var(--cor-texto-invertido)',
    boxShadow: '0 1px 2px rgba(34,31,26,0.10)'
  },
  secundario: {
    background: 'var(--cor-superficie)',
    color: 'var(--cor-texto)',
    borderColor: 'var(--cor-borda-forte)'
  },
  texto: {
    background: 'transparent',
    color: 'var(--cor-acao)'
  },
  perigo: {
    background: 'var(--cor-erro)',
    color: 'var(--cor-texto-invertido)'
  }
};
const HOVER = {
  primario: {
    background: 'var(--cor-acao-hover)',
    boxShadow: '0 6px 14px rgba(38,64,90,0.22)',
    transform: 'translateY(-1px)'
  },
  secundario: {
    background: 'var(--cor-superficie-2)',
    borderColor: 'var(--cor-borda-controle-hover)'
  },
  texto: {
    background: 'var(--cor-acao-suave)'
  },
  perigo: {
    filter: 'brightness(0.92)'
  }
};
const ATIVO = {
  primario: {
    background: 'var(--cor-acao-ativa)',
    boxShadow: 'inset 0 2px 4px rgba(0,0,0,0.18)',
    transform: 'translateY(1px)'
  },
  secundario: {
    background: 'var(--cor-superficie-2)',
    borderColor: 'var(--cor-borda-controle-hover)',
    boxShadow: 'inset 0 1px 2px rgba(34,31,26,0.10)'
  },
  texto: {
    background: 'var(--cor-acao-suave-2)',
    color: 'var(--cor-acao-ativa)'
  },
  perigo: {
    filter: 'brightness(0.84)'
  }
};
const FOCO = {
  primario: {
    boxShadow: '0 0 0 2px var(--cor-superficie), 0 0 0 5px var(--cor-acao-ativa)'
  },
  secundario: {
    borderColor: 'var(--cor-acao)',
    boxShadow: 'var(--anel-foco)'
  },
  texto: {
    boxShadow: 'var(--anel-foco)'
  },
  perigo: {
    boxShadow: 'var(--anel-foco-erro)'
  }
};
const TAMANHOS = {
  pequeno: {
    fontSize: 13,
    padding: '6px 12px',
    borderRadius: 'var(--raio-pequeno)'
  },
  medio: {
    fontSize: 15,
    padding: '10px 18px',
    borderRadius: 'var(--raio-medio)'
  },
  grande: {
    fontSize: 18,
    padding: '13px 24px',
    borderRadius: 'var(--raio-medio)'
  }
};
function Botao({
  variante = 'primario',
  tamanho = 'medio',
  bloco,
  desabilitado,
  carregando,
  onClick,
  type = 'button',
  children,
  style
}) {
  const [hover, setHover] = useState(false);
  const [ativo, setAtivo] = useState(false);
  const [foco, setFoco] = useState(false);
  const inerte = desabilitado || carregando;
  let estado = {};
  if (!inerte) {
    if (ativo) estado = ATIVO[variante];else if (hover) estado = HOVER[variante];
    if (foco) estado = {
      ...estado,
      ...FOCO[variante]
    };
  }
  return React.createElement('button', {
    type,
    disabled: inerte,
    onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => {
      setHover(false);
      setAtivo(false);
    },
    onMouseDown: () => setAtivo(true),
    onMouseUp: () => setAtivo(false),
    onFocus: () => setFoco(true),
    onBlur: () => setFoco(false),
    style: {
      display: bloco ? 'flex' : 'inline-flex',
      width: bloco ? '100%' : undefined,
      alignItems: 'center',
      justifyContent: 'center',
      gap: 8,
      fontFamily: 'var(--fonte-ui)',
      fontWeight: 600,
      border: '1px solid transparent',
      outline: 'none',
      whiteSpace: 'nowrap',
      cursor: inerte ? 'not-allowed' : 'pointer',
      opacity: inerte ? 0.45 : 1,
      transition: 'background 150ms ease, border-color 150ms ease, box-shadow 150ms ease, transform 120ms ease, color 150ms ease',
      ...TAMANHOS[tamanho],
      ...VARIANTES[variante],
      ...estado,
      ...style
    }
  }, carregando ? React.createElement(Girador, {
    key: 'g'
  }) : null, children);
}
function Girador() {
  return React.createElement('span', {
    style: {
      width: 13,
      height: 13,
      borderRadius: 999,
      flex: 'none',
      border: '2px solid currentColor',
      borderTopColor: 'transparent',
      animation: 'aa-girar 0.7s linear infinite'
    }
  });
}
Object.assign(__ds_scope, { Botao });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/acoes/Botao.jsx", error: String((e && e.message) || e) }); }

// components/acoes/Dica.jsx
try { (() => {
/* global React */
const {
  useState
} = React;
const POS = {
  cima: {
    bottom: '100%',
    left: '50%',
    transform: 'translate(-50%, -8px)'
  },
  baixo: {
    top: '100%',
    left: '50%',
    transform: 'translate(-50%, 8px)'
  },
  esquerda: {
    right: '100%',
    top: '50%',
    transform: 'translate(-8px, -50%)'
  },
  direita: {
    left: '100%',
    top: '50%',
    transform: 'translate(8px, -50%)'
  }
};
function Dica({
  texto,
  posicao = 'cima',
  children
}) {
  const [ver, setVer] = useState(false);
  return React.createElement('span', {
    style: {
      position: 'relative',
      display: 'inline-flex'
    },
    onMouseEnter: () => setVer(true),
    onMouseLeave: () => setVer(false),
    onFocus: () => setVer(true),
    onBlur: () => setVer(false)
  }, children, ver ? React.createElement('span', {
    key: 't',
    role: 'tooltip',
    style: {
      position: 'absolute',
      zIndex: 100,
      ...POS[posicao],
      background: 'var(--cor-camada-invertida)',
      color: 'var(--cor-superficie)',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 12,
      lineHeight: 1.4,
      padding: '6px 10px',
      borderRadius: 'var(--raio-pequeno)',
      whiteSpace: 'nowrap',
      boxShadow: 'var(--sombra-media)',
      pointerEvents: 'none',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)'
    }
  }, texto) : null);
}
Object.assign(__ds_scope, { Dica });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/acoes/Dica.jsx", error: String((e && e.message) || e) }); }

// components/acoes/Etiqueta.jsx
try { (() => {
/* global React */
const TONS = {
  neutro: {
    background: 'var(--cor-superficie-2)',
    color: 'var(--cor-texto-suave)'
  },
  info: {
    background: 'var(--cor-info-bg)',
    color: 'var(--cor-info)'
  },
  sucesso: {
    background: 'var(--cor-sucesso-bg)',
    color: 'var(--cor-sucesso)'
  },
  atencao: {
    background: 'var(--cor-atencao-bg)',
    color: 'var(--cor-atencao)'
  },
  erro: {
    background: 'var(--cor-erro-bg)',
    color: 'var(--cor-erro)'
  },
  destaque: {
    background: 'var(--cor-destaque-bg)',
    color: 'var(--cor-destaque-texto)'
  }
};
function Etiqueta({
  variante = 'neutro',
  ponto = true,
  children,
  style
}) {
  return React.createElement('span', {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 11,
      fontWeight: 600,
      padding: '4px 12px',
      borderRadius: 999,
      whiteSpace: 'nowrap',
      ...TONS[variante],
      ...style
    }
  }, ponto ? React.createElement('span', {
    key: 'p',
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: 'currentColor',
      opacity: 0.75
    }
  }) : null, children);
}
Object.assign(__ds_scope, { Etiqueta });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/acoes/Etiqueta.jsx", error: String((e && e.message) || e) }); }

// components/dados/Esqueleto.jsx
try { (() => {
/* global React */
function Esqueleto({
  linhas = 1,
  altura = 12,
  largura = '100%',
  formato = 'linha',
  style
}) {
  const raio = formato === 'circulo' ? 999 : formato === 'bloco' ? 'var(--raio-medio)' : 'var(--raio-pequeno)';
  const itens = Array.from({
    length: linhas
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      ...style
    }
  }, itens.map((_, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      height: formato === 'circulo' ? altura : altura,
      width: i === itens.length - 1 && linhas > 1 ? '65%' : largura,
      background: 'var(--cor-superficie-2)',
      borderRadius: raio,
      animation: 'aa-pulso 1.4s ease-in-out infinite',
      animationDelay: i * 0.12 + 's'
    }
  })));
}
Object.assign(__ds_scope, { Esqueleto });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dados/Esqueleto.jsx", error: String((e && e.message) || e) }); }

// components/dados/Metrica.jsx
try { (() => {
/* global React */
/* Só token semântico: literal de cor aqui não vira no tema escuro, e o
   delta de uma métrica é texto pequeno — é onde a falha aparece primeiro. */
const TENDENCIAS = {
  alta: 'var(--cor-sucesso)',
  baixa: 'var(--cor-erro)',
  neutra: 'var(--cor-texto-suave)',
  atencao: 'var(--cor-atencao)'
};
function Metrica({
  rotulo,
  valor,
  delta,
  tendencia = 'neutra',
  serie,
  unidade,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: 'var(--cor-superficie)',
      border: '1px solid var(--cor-borda)',
      borderRadius: 'var(--raio-medio)',
      padding: 16,
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      textTransform: 'uppercase',
      letterSpacing: '0.06em',
      color: 'var(--cor-texto-suave)'
    }
  }, rotulo), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'baseline',
      gap: 6,
      marginTop: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--fonte-display)',
      fontSize: 36,
      fontWeight: 600,
      lineHeight: 1.05,
      color: 'var(--cor-texto-forte)'
    }
  }, valor), unidade ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--cor-texto-suave)'
    }
  }, unidade) : null), serie && serie.length > 1 ? /*#__PURE__*/React.createElement(Faisca, {
    serie: serie,
    cor: TENDENCIAS[tendencia]
  }) : null, delta ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      marginTop: 4,
      color: TENDENCIAS[tendencia]
    }
  }, delta) : null);
}
function Faisca({
  serie,
  cor
}) {
  const w = 100,
    h = 22;
  const min = Math.min(...serie),
    max = Math.max(...serie),
    span = max - min || 1;
  const pts = serie.map((v, i) => [i / (serie.length - 1) * w, h - (v - min) / span * (h - 3) - 1.5]);
  const d = pts.map((p, i) => (i ? 'L' : 'M') + p[0].toFixed(1) + ' ' + p[1].toFixed(1)).join(' ');
  return /*#__PURE__*/React.createElement("svg", {
    viewBox: '0 0 ' + w + ' ' + h,
    preserveAspectRatio: "none",
    style: {
      width: '100%',
      height: 22,
      marginTop: 6,
      display: 'block'
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: d,
    fill: "none",
    stroke: cor,
    strokeWidth: "1.6",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    vectorEffect: "non-scaling-stroke"
  }));
}
Object.assign(__ds_scope, { Metrica });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dados/Metrica.jsx", error: String((e && e.message) || e) }); }

// components/dados/Paginacao.jsx
try { (() => {
/* global React */
function Paginacao({
  pagina = 1,
  total = 1,
  aoMudar,
  style
}) {
  const paginas = [];
  const add = p => paginas.push(p);
  if (total <= 7) {
    for (let i = 1; i <= total; i++) add(i);
  } else {
    add(1);
    if (pagina > 3) add('…');
    for (let i = Math.max(2, pagina - 1); i <= Math.min(total - 1, pagina + 1); i++) add(i);
    if (pagina < total - 2) add('…');
    add(total);
  }
  const btn = (conteudo, alvo, ativo, chave) => /*#__PURE__*/React.createElement("button", {
    key: chave,
    disabled: alvo == null,
    onClick: () => alvo != null && aoMudar && aoMudar(alvo),
    style: {
      minWidth: 32,
      height: 32,
      padding: '0 8px',
      borderRadius: 'var(--raio-pequeno)',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 13,
      fontWeight: 600,
      border: '1px solid ' + (ativo ? 'var(--cor-acao)' : 'transparent'),
      background: ativo ? 'var(--cor-acao-suave)' : 'transparent',
      color: alvo == null ? 'var(--cor-borda-forte)' : ativo ? 'var(--cor-acao)' : 'var(--cor-texto-suave)',
      cursor: alvo == null ? 'default' : 'pointer',
      transition: 'background 150ms ease'
    }
  }, conteudo);
  return /*#__PURE__*/React.createElement("nav", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 4,
      ...style
    }
  }, btn('\u2039', pagina > 1 ? pagina - 1 : null, false, 'prev'), paginas.map((p, i) => p === '…' ? btn('…', null, false, 'e' + i) : btn(p, p, p === pagina, p)), btn('\u203A', pagina < total ? pagina + 1 : null, false, 'next'));
}
Object.assign(__ds_scope, { Paginacao });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dados/Paginacao.jsx", error: String((e && e.message) || e) }); }

// components/dados/Progresso.jsx
try { (() => {
/* global React */
function Progresso({
  valor = 0,
  max = 100,
  rotulo,
  formato = 'barra',
  cor = 'var(--cor-acao)',
  tamanho = 64,
  mostrarValor,
  style
}) {
  const pct = Math.max(0, Math.min(1, valor / (max || 1)));
  if (formato === 'anel') {
    const r = (tamanho - 8) / 2,
      c = 2 * Math.PI * r;
    return /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'inline-flex',
        flexDirection: 'column',
        alignItems: 'center',
        gap: 6,
        fontFamily: 'var(--fonte-ui)',
        ...style
      }
    }, /*#__PURE__*/React.createElement("svg", {
      width: tamanho,
      height: tamanho,
      style: {
        transform: 'rotate(-90deg)'
      }
    }, /*#__PURE__*/React.createElement("circle", {
      cx: tamanho / 2,
      cy: tamanho / 2,
      r: r,
      fill: "none",
      stroke: "var(--cor-superficie-2)",
      strokeWidth: "6"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: tamanho / 2,
      cy: tamanho / 2,
      r: r,
      fill: "none",
      stroke: cor,
      strokeWidth: "6",
      strokeLinecap: "round",
      strokeDasharray: c,
      strokeDashoffset: c * (1 - pct),
      style: {
        transition: 'stroke-dashoffset 240ms ease'
      }
    })), rotulo ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 11,
        color: 'var(--cor-texto-suave)'
      }
    }, rotulo) : null);
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, rotulo || mostrarValor ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      fontSize: 12,
      color: 'var(--cor-texto-suave)'
    }
  }, /*#__PURE__*/React.createElement("span", null, rotulo), mostrarValor ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontVariantNumeric: 'tabular-nums',
      fontWeight: 600
    }
  }, Math.round(pct * 100), "%") : null) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      height: 6,
      background: 'var(--cor-superficie-2)',
      borderRadius: 999,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      height: '100%',
      width: pct * 100 + '%',
      background: cor,
      borderRadius: 999,
      transition: 'width 240ms ease'
    }
  })));
}
Object.assign(__ds_scope, { Progresso });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dados/Progresso.jsx", error: String((e && e.message) || e) }); }

// components/dados/Tabela.jsx
try { (() => {
/* global React */
const {
  useState
} = React;

/* Estados de carregamento e erro moram AQUI dentro, não no consumidor.
   Toda tabela real tem três estados além do normal — vazio, carregando e
   erro — e resolver isso fora significa reescrever a mesma coisa em cada
   tela, quase sempre com espaçamento diferente. */
function Tabela({
  colunas = [],
  linhas = [],
  ordenavel,
  vazio = 'Nada por aqui ainda.',
  estado = 'pronto',
  erro,
  aoTentarNovamente,
  linhasCarregando = 5,
  aoClicarLinha,
  linhaAtiva,
  style
}) {
  const [ordem, setOrdem] = useState(null);
  const [hover, setHover] = useState(null);
  const dados = React.useMemo(() => {
    if (!ordem) return linhas;
    const dir = ordem.dir === 'asc' ? 1 : -1;
    return [...linhas].sort((a, b) => {
      const x = a[ordem.chave],
        y = b[ordem.chave];
      if (typeof x === 'number' && typeof y === 'number') return (x - y) * dir;
      return String(x ?? '').localeCompare(String(y ?? ''), 'pt-BR') * dir;
    });
  }, [linhas, ordem]);
  const alinhar = c => c.numerica ? 'right' : 'left';
  const cel = {
    padding: '12px 16px',
    borderBottom: '1px solid var(--cor-borda)',
    color: 'var(--cor-texto)',
    verticalAlign: 'middle'
  };
  const cabecalho = /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, colunas.map(c => /*#__PURE__*/React.createElement("th", {
    key: c.chave,
    scope: "col",
    onClick: () => estado === 'pronto' && ordenavel && setOrdem(o => o && o.chave === c.chave ? {
      chave: c.chave,
      dir: o.dir === 'asc' ? 'desc' : 'asc'
    } : {
      chave: c.chave,
      dir: 'asc'
    }),
    "aria-sort": ordenavel ? ordem && ordem.chave === c.chave ? ordem.dir === 'asc' ? 'ascending' : 'descending' : 'none' : undefined,
    style: {
      textAlign: alinhar(c),
      fontSize: 12,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-suave)',
      whiteSpace: 'nowrap',
      padding: '12px 16px',
      borderBottom: '1px solid var(--cor-borda-forte)',
      cursor: ordenavel && estado === 'pronto' ? 'pointer' : 'default',
      userSelect: 'none',
      width: c.largura
    }
  }, c.rotulo, ordenavel && ordem && ordem.chave === c.chave ? /*#__PURE__*/React.createElement("span", {
    style: {
      marginLeft: 5,
      color: 'var(--cor-acao)'
    }
  }, ordem.dir === 'asc' ? '\u2191' : '\u2193') : null))));
  const tabela = {
    width: '100%',
    borderCollapse: 'collapse',
    fontFamily: 'var(--fonte-ui)',
    fontSize: 15,
    ...style
  };

  /* Carregando: esqueleto com a MESMA grade da tabela pronta, para o
     conteúdo não saltar quando chegar. Largura das barras varia de
     propósito — barra idêntica em toda célula parece defeito. */
  if (estado === 'carregando') {
    return /*#__PURE__*/React.createElement("table", {
      style: tabela,
      "aria-busy": "true"
    }, cabecalho, /*#__PURE__*/React.createElement("tbody", null, Array.from({
      length: linhasCarregando
    }).map((_, i) => /*#__PURE__*/React.createElement("tr", {
      key: i
    }, colunas.map((c, j) => /*#__PURE__*/React.createElement("td", {
      key: c.chave,
      style: {
        ...cel,
        borderBottom: i === linhasCarregando - 1 ? 'none' : cel.borderBottom
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'block',
        height: 11,
        borderRadius: 'var(--raio-pequeno)',
        width: [78, 52, 64, 44][(i + j) % 4] + '%',
        marginLeft: c.numerica ? 'auto' : 0,
        background: 'var(--cor-superficie-2)',
        animation: 'aa-pulso 1.4s ease-in-out infinite',
        animationDelay: i * 90 + 'ms'
      }
    })))))));
  }

  /* Erro: sempre com o próximo passo. Uma tabela que só diz "erro"
     deixa a pessoa sem saída. */
  if (estado === 'erro') {
    return /*#__PURE__*/React.createElement("table", {
      style: tabela
    }, cabecalho, /*#__PURE__*/React.createElement("tbody", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
      colSpan: colunas.length,
      style: {
        padding: '40px 16px',
        textAlign: 'center'
      },
      role: "alert"
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontSize: 15,
        fontWeight: 600,
        color: 'var(--cor-texto-forte)'
      }
    }, "N\xE3o foi poss\xEDvel carregar"), /*#__PURE__*/React.createElement("div", {
      style: {
        fontSize: 13,
        color: 'var(--cor-texto-suave)',
        margin: '4px auto 0',
        maxWidth: '46ch'
      }
    }, erro || 'A conexão falhou no meio do caminho. Seus dados estão a salvo.'), aoTentarNovamente ? /*#__PURE__*/React.createElement("button", {
      type: "button",
      onClick: aoTentarNovamente,
      style: {
        marginTop: 16,
        font: 'inherit',
        fontSize: 13,
        fontWeight: 600,
        cursor: 'pointer',
        padding: '8px 16px',
        borderRadius: 'var(--raio-pequeno)',
        border: '1px solid var(--cor-borda-controle)',
        background: 'var(--cor-superficie)',
        color: 'var(--cor-texto)'
      }
    }, "Tentar de novo") : null))));
  }
  return /*#__PURE__*/React.createElement("table", {
    style: tabela
  }, cabecalho, /*#__PURE__*/React.createElement("tbody", null, dados.length === 0 ? /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    colSpan: colunas.length,
    style: {
      padding: '32px 16px',
      textAlign: 'center',
      color: 'var(--cor-texto-suave)',
      fontSize: 13
    }
  }, vazio)) : dados.map((l, i) => {
    const ativa = linhaAtiva != null && (l.id ?? i) === linhaAtiva;
    return /*#__PURE__*/React.createElement("tr", {
      key: l.id ?? i,
      onMouseEnter: () => setHover(i),
      onMouseLeave: () => setHover(null),
      onClick: () => aoClicarLinha && aoClicarLinha(l),
      "aria-selected": ativa || undefined,
      style: {
        background: ativa ? 'var(--cor-superficie-2)' : hover === i ? 'var(--cor-linha-hover)' : 'transparent',
        boxShadow: ativa ? 'inset 3px 0 0 var(--cor-acao)' : 'none',
        cursor: aoClicarLinha ? 'pointer' : 'default',
        transition: 'background 150ms ease'
      }
    }, colunas.map(c => /*#__PURE__*/React.createElement("td", {
      key: c.chave,
      style: {
        ...cel,
        textAlign: alinhar(c),
        borderBottom: i === dados.length - 1 ? 'none' : cel.borderBottom,
        fontVariantNumeric: c.numerica ? 'tabular-nums' : 'normal'
      }
    }, c.render ? c.render(l) : l[c.chave])));
  })));
}
Object.assign(__ds_scope, { Tabela });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dados/Tabela.jsx", error: String((e && e.message) || e) }); }

// components/dados/Vazio.jsx
try { (() => {
/* global React */
function Vazio({
  titulo,
  descricao,
  acao,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 8,
      padding: '48px 24px',
      textAlign: 'center',
      fontFamily: 'var(--fonte-ui)',
      border: '1px dashed var(--cor-borda-forte)',
      borderRadius: 'var(--raio-grande)',
      background: 'var(--cor-superficie)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 28,
      height: 28,
      borderRadius: 999,
      border: '1px solid var(--cor-borda-forte)',
      marginBottom: 4
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 18,
      fontWeight: 600,
      color: 'var(--cor-texto-forte)'
    }
  }, titulo), descricao ? /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontSize: 13,
      color: 'var(--cor-texto-suave)',
      maxWidth: '44ch',
      lineHeight: 1.55
    }
  }, descricao) : null, acao ? /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 8
    }
  }, acao) : null);
}
Object.assign(__ds_scope, { Vazio });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/dados/Vazio.jsx", error: String((e && e.message) || e) }); }

// components/estrutura/Abas.jsx
try { (() => {
/* global React */
function Abas({
  abas = [],
  ativa,
  aoMudar,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    role: "tablist",
    style: {
      display: 'flex',
      gap: 24,
      borderBottom: '1px solid var(--cor-borda)',
      ...style
    }
  }, abas.map(a => {
    const id = typeof a === 'string' ? a : a.id;
    const rotulo = typeof a === 'string' ? a : a.rotulo;
    const contagem = typeof a === 'string' ? null : a.contagem;
    const sel = id === ativa;
    return /*#__PURE__*/React.createElement("button", {
      key: id,
      role: "tab",
      "aria-selected": sel,
      onClick: () => aoMudar && aoMudar(id),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 8,
        background: 'none',
        border: 'none',
        padding: '12px 0',
        cursor: 'pointer',
        fontFamily: 'var(--fonte-ui)',
        fontSize: 15,
        fontWeight: 600,
        color: sel ? 'var(--cor-texto-forte)' : 'var(--cor-texto-suave)',
        borderBottom: '2px solid ' + (sel ? 'var(--cor-acao)' : 'transparent'),
        marginBottom: -1,
        transition: 'color 150ms ease, border-color 150ms ease'
      }
    }, rotulo, contagem != null ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 11,
        fontWeight: 600,
        padding: '2px 7px',
        borderRadius: 999,
        background: sel ? 'var(--cor-acao-suave)' : 'var(--cor-superficie-2)',
        color: sel ? 'var(--cor-acao)' : 'var(--cor-texto-suave)'
      }
    }, contagem) : null);
  }));
}
Object.assign(__ds_scope, { Abas });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/estrutura/Abas.jsx", error: String((e && e.message) || e) }); }

// components/estrutura/Cartao.jsx
try { (() => {
/* global React */
function Cartao({
  titulo,
  rotulo,
  acoes,
  rodape,
  compacto,
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("section", {
    style: {
      background: 'var(--cor-superficie)',
      border: '1px solid var(--cor-borda)',
      borderRadius: compacto ? 'var(--raio-medio)' : 'var(--raio-grande)',
      padding: compacto ? 16 : 24,
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, rotulo || titulo || acoes ? /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      justifyContent: 'space-between',
      gap: 16,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, rotulo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-suave)'
    }
  }, rotulo) : null, titulo ? /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontSize: 18,
      fontWeight: 600,
      lineHeight: 1.35,
      color: 'var(--cor-texto-forte)'
    }
  }, titulo) : null), acoes ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 8,
      flex: 'none'
    }
  }, acoes) : null) : null, children, rodape ? /*#__PURE__*/React.createElement("footer", {
    style: {
      marginTop: 16,
      paddingTop: 16,
      borderTop: '1px solid var(--cor-borda)',
      display: 'flex',
      gap: 12,
      justifyContent: 'flex-end'
    }
  }, rodape) : null);
}
Object.assign(__ds_scope, { Cartao });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/estrutura/Cartao.jsx", error: String((e && e.message) || e) }); }

// components/estrutura/Divisor.jsx
try { (() => {
/* global React */
function Divisor({
  rotulo,
  style
}) {
  if (!rotulo) return /*#__PURE__*/React.createElement("hr", {
    style: {
      height: 1,
      background: 'var(--cor-borda)',
      border: 'none',
      margin: '24px 0',
      ...style
    }
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      margin: '24px 0',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 12,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-suave)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: 1,
      background: 'var(--cor-borda)'
    }
  }), rotulo, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      height: 1,
      background: 'var(--cor-borda)'
    }
  }));
}
Object.assign(__ds_scope, { Divisor });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/estrutura/Divisor.jsx", error: String((e && e.message) || e) }); }

// components/estrutura/Gaveta.jsx
try { (() => {
/* global React */
const {
  useEffect,
  useRef
} = React;
const LADOS = {
  direita: {
    inset: '0 0 0 auto',
    borda: 'borderLeft',
    anim: 'aa-gaveta-direita'
  },
  esquerda: {
    inset: '0 auto 0 0',
    borda: 'borderRight',
    anim: 'aa-gaveta-esquerda'
  },
  baixo: {
    inset: 'auto 0 0 0',
    borda: 'borderTop',
    anim: 'aa-gaveta-baixo'
  }
};

/* Gaveta: painel lateral para tarefa secundária que não merece sair da tela —
   filtros, detalhe de item, formulário curto. Se a tarefa é a única coisa que
   importa naquele momento, use Modal; se é navegação, use BarraLateral. */
function Gaveta({
  aberta = true,
  lado = 'direita',
  titulo,
  rotulo,
  aoFechar,
  largura = 400,
  altura = '70vh',
  acoes,
  children,
  style
}) {
  const painel = useRef(null);
  const l = LADOS[lado] || LADOS.direita;
  const deBaixo = lado === 'baixo';
  useEffect(() => {
    if (!aberta) return;
    const h = e => {
      if (e.key === 'Escape' && aoFechar) aoFechar();
    };
    window.addEventListener('keydown', h);
    const antes = document.activeElement;
    if (painel.current) painel.current.focus();
    return () => {
      window.removeEventListener('keydown', h);
      if (antes && antes.focus) antes.focus();
    };
  }, [aberta, aoFechar]);
  if (!aberta) return null;
  return /*#__PURE__*/React.createElement("div", {
    onClick: aoFechar,
    style: {
      position: 'fixed',
      inset: 0,
      zIndex: 'var(--z-modal)',
      background: 'var(--cor-veu)',
      animation: 'aa-veu-entra 160ms ease'
    }
  }, /*#__PURE__*/React.createElement("div", {
    ref: painel,
    role: "dialog",
    "aria-modal": "true",
    "aria-label": titulo || 'Painel',
    tabIndex: -1,
    onClick: e => e.stopPropagation(),
    style: {
      position: 'absolute',
      inset: l.inset,
      outline: 'none',
      width: deBaixo ? 'auto' : largura,
      maxWidth: '100%',
      height: deBaixo ? altura : 'auto',
      maxHeight: '100%',
      display: 'flex',
      flexDirection: 'column',
      background: 'var(--cor-camada)',
      boxShadow: 'var(--sombra-grande)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      [l.borda]: '1px solid var(--cor-borda-camada)',
      borderRadius: deBaixo ? 'var(--raio-grande) var(--raio-grande) 0 0' : 0,
      fontFamily: 'var(--fonte-ui)',
      animation: l.anim + ' 220ms cubic-bezier(0.22,0.61,0.36,1)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-start',
      gap: 16,
      padding: '20px 24px 16px',
      borderBottom: '1px solid var(--cor-borda)',
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, rotulo ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 12,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-suave)',
      marginBottom: 4
    }
  }, rotulo) : null, titulo ? /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: 'var(--fonte-display)',
      fontSize: 22,
      fontWeight: 600,
      lineHeight: 1.2,
      color: 'var(--cor-texto-forte)'
    }
  }, titulo) : null), aoFechar ? /*#__PURE__*/React.createElement("button", {
    onClick: aoFechar,
    "aria-label": "Fechar painel",
    style: {
      background: 'none',
      border: 'none',
      cursor: 'pointer',
      color: 'var(--cor-texto-suave)',
      fontSize: 20,
      lineHeight: 1,
      padding: 0,
      flex: 'none',
      minWidth: 44,
      minHeight: 44,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'flex-end'
    }
  }, "\xD7") : null), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflow: 'auto',
      padding: '20px 24px',
      fontSize: 15,
      lineHeight: 1.55,
      color: 'var(--cor-texto)'
    }
  }, children), acoes ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'flex-end',
      gap: 12,
      padding: '16px 24px',
      borderTop: '1px solid var(--cor-borda)',
      background: 'var(--cor-superficie-2)',
      flex: 'none'
    }
  }, acoes) : null));
}
Object.assign(__ds_scope, { Gaveta });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/estrutura/Gaveta.jsx", error: String((e && e.message) || e) }); }

// components/estrutura/Migalhas.jsx
try { (() => {
/* global React */
function Migalhas({
  itens = [],
  aoNavegar,
  style
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Trilha",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      flexWrap: 'wrap',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 13,
      color: 'var(--cor-texto-suave)',
      ...style
    }
  }, itens.map((it, i) => {
    const rotulo = typeof it === 'string' ? it : it.rotulo;
    const ultimo = i === itens.length - 1;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: i
    }, ultimo ? /*#__PURE__*/React.createElement("span", {
      style: {
        color: 'var(--cor-texto)',
        fontWeight: 600
      },
      "aria-current": "page"
    }, rotulo) : /*#__PURE__*/React.createElement("button", {
      onClick: () => aoNavegar && aoNavegar(it, i),
      style: {
        background: 'none',
        border: 'none',
        padding: 0,
        cursor: 'pointer',
        fontFamily: 'inherit',
        fontSize: 'inherit',
        color: 'var(--cor-acao)'
      }
    }, rotulo), !ultimo ? /*#__PURE__*/React.createElement("span", {
      style: {
        color: 'var(--cor-borda-forte)'
      }
    }, "/") : null);
  }));
}
Object.assign(__ds_scope, { Migalhas });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/estrutura/Migalhas.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Alerta.jsx
try { (() => {
/* global React */
const TONS = {
  info: {
    bg: 'var(--cor-info-bg)',
    fg: 'var(--cor-info)',
    ponto: 'var(--cor-info)'
  },
  sucesso: {
    bg: 'var(--cor-sucesso-bg)',
    fg: 'var(--cor-sucesso)',
    ponto: 'var(--cor-sucesso)'
  },
  atencao: {
    bg: 'var(--cor-atencao-bg)',
    fg: 'var(--cor-atencao)',
    ponto: 'var(--cor-atencao)'
  },
  erro: {
    bg: 'var(--cor-erro-bg)',
    fg: 'var(--cor-erro)',
    ponto: 'var(--cor-erro)'
  }
};
function Alerta({
  variante = 'info',
  titulo,
  children,
  aoFechar,
  style
}) {
  const t = TONS[variante] || TONS.info;
  return /*#__PURE__*/React.createElement("div", {
    role: "status",
    style: {
      display: 'flex',
      gap: 12,
      padding: '12px 16px',
      borderRadius: 'var(--raio-medio)',
      background: t.bg,
      color: t.fg,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 13,
      alignItems: 'flex-start',
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: t.ponto,
      marginTop: 6,
      flex: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      lineHeight: 1.5
    }
  }, titulo ? /*#__PURE__*/React.createElement("strong", {
    style: {
      fontWeight: 600
    }
  }, titulo) : null, /*#__PURE__*/React.createElement("span", null, children)), aoFechar ? /*#__PURE__*/React.createElement("button", {
    onClick: aoFechar,
    "aria-label": "Fechar",
    style: {
      background: 'none',
      border: 'none',
      cursor: 'pointer',
      color: 'inherit',
      opacity: 0.6,
      fontSize: 15,
      lineHeight: 1,
      padding: 0,
      flex: 'none'
    }
  }, "\xD7") : null);
}
Object.assign(__ds_scope, { Alerta });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Alerta.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Aviso.jsx
try { (() => {
/* global React */
const TONS = {
  info: {
    bg: 'var(--cor-info-bg)',
    fg: 'var(--cor-info)',
    ponto: 'var(--cor-info)'
  },
  sucesso: {
    bg: 'var(--cor-sucesso-bg)',
    fg: 'var(--cor-sucesso)',
    ponto: 'var(--cor-sucesso)'
  },
  atencao: {
    bg: 'var(--cor-atencao-bg)',
    fg: 'var(--cor-atencao)',
    ponto: 'var(--cor-atencao)'
  },
  erro: {
    bg: 'var(--cor-erro-bg)',
    fg: 'var(--cor-erro)',
    ponto: 'var(--cor-erro)'
  }
};
function Aviso({
  variante = 'info',
  titulo,
  mensagem,
  aoFechar,
  style
}) {
  const t = TONS[variante] || TONS.info;
  return /*#__PURE__*/React.createElement("div", {
    role: "status",
    style: {
      display: 'flex',
      gap: 12,
      alignItems: 'flex-start',
      width: 340,
      maxWidth: '100%',
      background: 'var(--cor-camada)',
      border: '1px solid var(--cor-borda-camada)',
      borderRadius: 'var(--raio-medio)',
      boxShadow: 'var(--sombra-media)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      padding: '14px 16px',
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: t.ponto,
      marginTop: 6,
      flex: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, titulo ? /*#__PURE__*/React.createElement("strong", {
    style: {
      fontSize: 14,
      fontWeight: 600,
      color: 'var(--cor-texto-forte)'
    }
  }, titulo) : null, mensagem ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      lineHeight: 1.5,
      color: 'var(--cor-texto-suave)'
    }
  }, mensagem) : null), aoFechar ? /*#__PURE__*/React.createElement("button", {
    onClick: aoFechar,
    "aria-label": "Fechar",
    style: {
      background: 'none',
      border: 'none',
      cursor: 'pointer',
      color: 'var(--cor-texto-suave)',
      fontSize: 15,
      lineHeight: 1,
      padding: 0,
      flex: 'none'
    }
  }, "\xD7") : null);
}
Object.assign(__ds_scope, { Aviso });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Aviso.jsx", error: String((e && e.message) || e) }); }

// components/feedback/MenuSuspenso.jsx
try { (() => {
/* global React */
const {
  useState,
  useRef,
  useEffect
} = React;
function MenuSuspenso({
  gatilho,
  rotulo = 'Opções',
  itens = [],
  aoEscolher,
  alinhamento = 'esquerda',
  style
}) {
  const [aberto, setAberto] = useState(false);
  const [hover, setHover] = useState(null);
  const ref = useRef(null);
  useEffect(() => {
    if (!aberto) return;
    const fora = e => {
      if (ref.current && !ref.current.contains(e.target)) setAberto(false);
    };
    document.addEventListener('mousedown', fora);
    return () => document.removeEventListener('mousedown', fora);
  }, [aberto]);
  return /*#__PURE__*/React.createElement("span", {
    ref: ref,
    style: {
      position: 'relative',
      display: 'inline-flex',
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    onClick: () => setAberto(a => !a),
    style: {
      display: 'inline-flex',
      cursor: 'pointer'
    }
  }, gatilho || /*#__PURE__*/React.createElement("button", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      fontWeight: 600,
      padding: '10px 16px',
      borderRadius: 'var(--raio-medio)',
      border: '1px solid var(--cor-borda-forte)',
      background: 'var(--cor-superficie)',
      color: 'var(--cor-texto)',
      cursor: 'pointer'
    }
  }, rotulo, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 9,
      color: 'var(--cor-texto-suave)'
    }
  }, "\u25BC"))), aberto ? /*#__PURE__*/React.createElement("div", {
    role: "menu",
    style: {
      position: 'absolute',
      top: 'calc(100% + 6px)',
      zIndex: 100,
      minWidth: 180,
      [alinhamento === 'direita' ? 'right' : 'left']: 0,
      background: 'var(--cor-camada)',
      border: '1px solid var(--cor-borda-camada)',
      borderRadius: 'var(--raio-medio)',
      boxShadow: 'var(--sombra-media)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      padding: 4
    }
  }, itens.map((it, i) => it.separador ? /*#__PURE__*/React.createElement("div", {
    key: 's' + i,
    style: {
      height: 1,
      background: 'var(--cor-borda)',
      margin: '4px 0'
    }
  }) : /*#__PURE__*/React.createElement("button", {
    key: it.id || i,
    role: "menuitem",
    onMouseEnter: () => setHover(i),
    onMouseLeave: () => setHover(null),
    onClick: () => {
      setAberto(false);
      aoEscolher && aoEscolher(it);
    },
    style: {
      display: 'block',
      width: '100%',
      textAlign: 'left',
      border: 'none',
      cursor: 'pointer',
      padding: '8px 12px',
      borderRadius: 'var(--raio-pequeno)',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 14,
      color: it.perigo ? 'var(--cor-erro)' : 'var(--cor-texto)',
      background: hover === i ? 'var(--cor-linha-hover)' : 'transparent'
    }
  }, it.rotulo))) : null);
}
Object.assign(__ds_scope, { MenuSuspenso });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/MenuSuspenso.jsx", error: String((e && e.message) || e) }); }

// components/feedback/Modal.jsx
try { (() => {
/* global React */
const {
  useEffect
} = React;
function Modal({
  aberto = true,
  titulo,
  rotulo,
  aoFechar,
  acoes,
  largura = 480,
  children
}) {
  useEffect(() => {
    if (!aberto) return;
    const h = e => {
      if (e.key === 'Escape' && aoFechar) aoFechar();
    };
    window.addEventListener('keydown', h);
    return () => window.removeEventListener('keydown', h);
  }, [aberto, aoFechar]);
  if (!aberto) return null;
  return /*#__PURE__*/React.createElement("div", {
    onClick: aoFechar,
    style: {
      position: 'fixed',
      inset: 0,
      zIndex: 'var(--z-modal)',
      background: 'var(--cor-veu)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      padding: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    role: "dialog",
    "aria-modal": "true",
    onClick: e => e.stopPropagation(),
    style: {
      width: largura,
      maxWidth: '100%',
      maxHeight: '86vh',
      overflow: 'auto',
      background: 'var(--cor-camada)',
      borderRadius: 'var(--raio-grande)',
      border: '1px solid var(--cor-borda-camada)',
      boxShadow: 'var(--sombra-grande)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      padding: 24,
      fontFamily: 'var(--fonte-ui)'
    }
  }, rotulo ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 12,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-suave)',
      marginBottom: 4
    }
  }, rotulo) : null, titulo ? /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: '0 0 16px',
      fontFamily: 'var(--fonte-display)',
      fontSize: 24,
      fontWeight: 600,
      lineHeight: 1.2,
      color: 'var(--cor-texto-forte)'
    }
  }, titulo) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      lineHeight: 1.55,
      color: 'var(--cor-texto)'
    }
  }, children), acoes ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'flex-end',
      gap: 12,
      marginTop: 24
    }
  }, acoes) : null));
}
Object.assign(__ds_scope, { Modal });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/Modal.jsx", error: String((e && e.message) || e) }); }

// components/feedback/PilhaAvisos.jsx
try { (() => {
/* global React */
const {
  useState,
  useCallback,
  useRef,
  useEffect
} = React;
const CANTOS = {
  'inferior-direito': {
    bottom: 'var(--espaco-5)',
    right: 'var(--espaco-5)',
    align: 'flex-end'
  },
  'inferior-esquerdo': {
    bottom: 'var(--espaco-5)',
    left: 'var(--espaco-5)',
    align: 'flex-start'
  },
  'superior-direito': {
    top: 'var(--espaco-5)',
    right: 'var(--espaco-5)',
    align: 'flex-end'
  },
  'superior-esquerdo': {
    top: 'var(--espaco-5)',
    left: 'var(--espaco-5)',
    align: 'flex-start'
  }
};
const TONS = {
  info: 'var(--cor-info)',
  sucesso: 'var(--cor-sucesso)',
  atencao: 'var(--cor-atencao)',
  erro: 'var(--cor-erro)'
};

/* Fila de avisos: no máximo `limite` na tela, o mais novo empurra o mais antigo.
   Erro não some sozinho — quem errou precisa ler. */
function usarAvisos({
  limite = 3,
  duracao = 5000
} = {}) {
  const [avisos, setAvisos] = useState([]);
  const seq = useRef(0);
  const dispensar = useCallback(id => setAvisos(a => a.filter(x => x.id !== id)), []);
  const avisar = useCallback(aviso => {
    const id = ++seq.current;
    setAvisos(a => [...a, {
      id,
      variante: 'info',
      ...aviso
    }].slice(-limite));
    return id;
  }, [limite]);
  return {
    avisos,
    avisar,
    dispensar
  };
}
function Item({
  aviso,
  aoDispensar,
  duracao
}) {
  const permanente = aviso.variante === 'erro' || aviso.acao;
  useEffect(() => {
    if (permanente) return;
    const t = setTimeout(aoDispensar, aviso.duracao ?? duracao);
    return () => clearTimeout(t);
  }, [permanente, aviso.duracao, duracao, aoDispensar]);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12,
      alignItems: 'flex-start',
      width: 340,
      maxWidth: '100%',
      boxSizing: 'border-box',
      background: 'var(--cor-camada)',
      border: '1px solid var(--cor-borda-camada)',
      borderRadius: 'var(--raio-medio)',
      boxShadow: 'var(--sombra-media)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      padding: '14px 16px',
      fontFamily: 'var(--fonte-ui)',
      pointerEvents: 'auto',
      animation: 'aa-aviso-entra 200ms ease'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: TONS[aviso.variante] || TONS.info,
      marginTop: 6,
      flex: 'none'
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, aviso.titulo ? /*#__PURE__*/React.createElement("strong", {
    style: {
      fontSize: 14,
      fontWeight: 600,
      color: 'var(--cor-texto-forte)'
    }
  }, aviso.titulo) : null, aviso.mensagem ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      lineHeight: 1.5,
      color: 'var(--cor-texto-suave)'
    }
  }, aviso.mensagem) : null, aviso.acao ? /*#__PURE__*/React.createElement("button", {
    onClick: () => {
      aviso.acao.ao && aviso.acao.ao();
      aoDispensar();
    },
    style: {
      alignSelf: 'flex-start',
      marginTop: 6,
      background: 'none',
      border: 'none',
      padding: 0,
      cursor: 'pointer',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-acao)',
      textDecoration: 'underline',
      textDecorationColor: 'var(--cor-link-sublinhado)',
      textUnderlineOffset: 3
    }
  }, aviso.acao.rotulo) : null), /*#__PURE__*/React.createElement("button", {
    onClick: aoDispensar,
    "aria-label": "Fechar aviso",
    style: {
      background: 'none',
      border: 'none',
      cursor: 'pointer',
      color: 'var(--cor-texto-suave)',
      fontSize: 15,
      lineHeight: 1,
      padding: 0,
      flex: 'none',
      minWidth: 20,
      minHeight: 20
    }
  }, "\xD7"));
}
function PilhaAvisos({
  avisos = [],
  aoDispensar,
  canto = 'inferior-direito',
  duracao = 5000,
  style
}) {
  const c = CANTOS[canto] || CANTOS['inferior-direito'];
  const deBaixo = canto.startsWith('inferior');
  return /*#__PURE__*/React.createElement("div", {
    "aria-live": "polite",
    "aria-atomic": "false",
    style: {
      position: 'fixed',
      zIndex: 'var(--z-aviso)',
      display: 'flex',
      flexDirection: deBaixo ? 'column-reverse' : 'column',
      alignItems: c.align,
      gap: 'var(--espaco-3)',
      pointerEvents: 'none',
      top: c.top,
      bottom: c.bottom,
      left: c.left,
      right: c.right,
      ...style
    }
  }, avisos.map(a => /*#__PURE__*/React.createElement(Item, {
    key: a.id,
    aviso: a,
    duracao: duracao,
    aoDispensar: () => aoDispensar && aoDispensar(a.id)
  })));
}

/* O compilador só expõe nomes com maiúscula e reencapsula a função exportada,
   então propriedade pendurada no componente não sobrevive. O hook sai como
   binding próprio, capitalizado: Avisos.usar(). */
const Avisos = {
  usar: usarAvisos
};
Object.assign(__ds_scope, { PilhaAvisos, Avisos });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/feedback/PilhaAvisos.jsx", error: String((e && e.message) || e) }); }

// components/formulario/AreaTexto.jsx
try { (() => {
/* global React */
const {
  useState
} = React;
function Envoltorio({
  rotulo,
  obrigatorio,
  dica,
  erro,
  children
}) {
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      fontFamily: 'var(--fonte-ui)'
    }
  }, rotulo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-texto)'
    }
  }, rotulo, obrigatorio ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--cor-destaque)',
      marginLeft: 2
    }
  }, "*") : null) : null, children, erro ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-erro)',
      fontWeight: 600
    }
  }, erro) : dica ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)'
    }
  }, dica) : null);
}
function entradaEstilo({
  foco,
  erro,
  desabilitado
}) {
  return {
    fontFamily: 'var(--fonte-ui)',
    fontSize: 15,
    color: 'var(--cor-texto)',
    padding: '9px 12px',
    width: '100%',
    boxSizing: 'border-box',
    border: '1px solid ' + (erro ? 'var(--cor-erro)' : foco ? 'var(--cor-acao)' : 'var(--cor-borda-forte)'),
    borderRadius: 'var(--raio-pequeno)',
    background: desabilitado ? 'var(--cor-superficie-2)' : 'var(--cor-superficie)',
    opacity: desabilitado ? 0.55 : 1,
    outline: 'none',
    boxShadow: foco ? erro ? 'var(--anel-foco-erro)' : 'var(--anel-foco)' : 'none',
    transition: 'border-color 150ms ease, box-shadow 150ms ease'
  };
}
function AreaTexto({
  rotulo,
  valor,
  aoMudar,
  placeholder,
  dica,
  erro,
  linhas = 4,
  obrigatorio,
  desabilitado,
  style
}) {
  const [foco, setFoco] = useState(false);
  return /*#__PURE__*/React.createElement(Envoltorio, {
    rotulo: rotulo,
    obrigatorio: obrigatorio,
    dica: dica,
    erro: erro
  }, /*#__PURE__*/React.createElement("textarea", {
    rows: linhas,
    value: valor ?? '',
    placeholder: placeholder,
    disabled: desabilitado,
    onChange: e => aoMudar && aoMudar(e.target.value),
    onFocus: () => setFoco(true),
    onBlur: () => setFoco(false),
    style: {
      ...entradaEstilo({
        foco,
        erro,
        desabilitado
      }),
      lineHeight: 1.55,
      resize: 'vertical',
      ...style
    }
  }));
}
Object.assign(__ds_scope, { AreaTexto });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/AreaTexto.jsx", error: String((e && e.message) || e) }); }

// components/formulario/Busca.jsx
try { (() => {
/* global React */
const {
  useState
} = React;
function Busca({
  valor,
  aoMudar,
  placeholder = 'Buscar…',
  largura = 260,
  style
}) {
  const [foco, setFoco] = useState(false);
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      width: largura,
      boxSizing: 'border-box',
      padding: '0 12px',
      height: 38,
      background: 'var(--cor-superficie)',
      border: '1px solid ' + (foco ? 'var(--cor-acao)' : 'var(--cor-borda-forte)'),
      borderRadius: 'var(--raio-pilula)',
      boxShadow: foco ? 'var(--anel-foco)' : 'none',
      transition: 'border-color 150ms ease, box-shadow 150ms ease',
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "15",
    height: "15",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "var(--cor-texto-suave)",
    strokeWidth: "2",
    strokeLinecap: "round",
    style: {
      flex: 'none'
    }
  }, /*#__PURE__*/React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "7"
  }), /*#__PURE__*/React.createElement("path", {
    d: "M20 20l-3.5-3.5"
  })), /*#__PURE__*/React.createElement("input", {
    value: valor ?? '',
    placeholder: placeholder,
    onChange: e => aoMudar && aoMudar(e.target.value),
    onFocus: () => setFoco(true),
    onBlur: () => setFoco(false),
    style: {
      flex: 1,
      minWidth: 0,
      border: 'none',
      outline: 'none',
      background: 'transparent',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 14,
      color: 'var(--cor-texto)'
    }
  }), valor ? /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: () => aoMudar && aoMudar(''),
    "aria-label": "Limpar",
    style: {
      border: 'none',
      background: 'none',
      cursor: 'pointer',
      color: 'var(--cor-texto-suave)',
      padding: 0,
      lineHeight: 1,
      fontSize: 15,
      flex: 'none'
    }
  }, "\xD7") : null);
}
Object.assign(__ds_scope, { Busca });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/Busca.jsx", error: String((e && e.message) || e) }); }

// components/formulario/Calendario.jsx
try { (() => {
/* global React */
const {
  useState
} = React;
const MESES = ['janeiro', 'fevereiro', 'março', 'abril', 'maio', 'junho', 'julho', 'agosto', 'setembro', 'outubro', 'novembro', 'dezembro'];
const SEMANA = ['D', 'S', 'T', 'Q', 'Q', 'S', 'S'];
const dia = d => new Date(d.getFullYear(), d.getMonth(), d.getDate());
const mesmoDia = (a, b) => a && b && dia(a).getTime() === dia(b).getTime();

/* Calendário de mês, pt-BR, semana começando no domingo (é o que o
   Brasil lê). Ele não sabe nada de "últimos 30 dias" — quem sabe disso
   é o SeletorPeriodo. Aqui só existe dia e intervalo. */
function Calendario({
  modo = 'dia',
  valor,
  aoSelecionar,
  min,
  max,
  mesInicial,
  style
}) {
  const base = mesInicial || (modo === 'intervalo' ? valor?.inicio : valor) || new Date();
  const [mes, setMes] = useState(new Date(base.getFullYear(), base.getMonth(), 1));
  const [pairando, setPairando] = useState(null);
  const inicio = modo === 'intervalo' ? valor?.inicio : valor;
  const fim = modo === 'intervalo' ? valor?.fim : null;
  /* Enquanto só a primeira ponta foi escolhida, o intervalo desenhado
     acompanha o mouse — sem isso a pessoa não entende que falta clicar. */
  const fimVisivel = fim || (modo === 'intervalo' && inicio && pairando > inicio ? pairando : null);
  const primeiro = new Date(mes.getFullYear(), mes.getMonth(), 1);
  const celulas = [];
  for (let i = 0; i < primeiro.getDay(); i++) celulas.push(null);
  const ultimo = new Date(mes.getFullYear(), mes.getMonth() + 1, 0).getDate();
  for (let d = 1; d <= ultimo; d++) celulas.push(new Date(mes.getFullYear(), mes.getMonth(), d));
  while (celulas.length % 7) celulas.push(null);
  const bloqueado = d => min && dia(d) < dia(min) || max && dia(d) > dia(max);
  function clicar(d) {
    if (bloqueado(d)) return;
    if (modo === 'dia') return aoSelecionar && aoSelecionar(d);
    if (!inicio || fim) return aoSelecionar && aoSelecionar({
      inicio: d,
      fim: null
    });
    /* Clicou antes do início? Não é erro — é a pessoa se corrigindo.
       Reordena em silêncio em vez de recusar o clique. */
    if (dia(d) < dia(inicio)) return aoSelecionar && aoSelecionar({
      inicio: d,
      fim: inicio
    });
    aoSelecionar && aoSelecionar({
      inicio,
      fim: d
    });
  }
  const seta = dir => /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-label": dir < 0 ? 'Mês anterior' : 'Próximo mês',
    onClick: () => setMes(new Date(mes.getFullYear(), mes.getMonth() + dir, 1)),
    style: {
      font: 'inherit',
      fontSize: 16,
      lineHeight: 1,
      cursor: 'pointer',
      width: 28,
      height: 28,
      flex: 'none',
      display: 'grid',
      placeItems: 'center',
      borderRadius: 'var(--raio-pequeno)',
      border: '1px solid var(--cor-borda)',
      background: 'var(--cor-superficie)',
      color: 'var(--cor-texto)'
    }
  }, dir < 0 ? '\u2039' : '\u203A');
  return (
    /*#__PURE__*/
    /* Largura máxima, não fixa: o calendário mora em barra lateral estreita
       tanto quanto em popover, e célula de tamanho fixo estoura a lateral. */
    React.createElement("div", {
      style: {
        fontFamily: 'var(--fonte-ui)',
        width: '100%',
        maxWidth: 266,
        minWidth: 0,
        boxSizing: 'border-box',
        ...style
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 4,
        marginBottom: 10
      }
    }, seta(-1), /*#__PURE__*/React.createElement("div", {
      style: {
        fontSize: 14,
        fontWeight: 600,
        color: 'var(--cor-texto-forte)',
        textTransform: 'capitalize',
        textAlign: 'center',
        minWidth: 0,
        whiteSpace: 'nowrap'
      }
    }, MESES[mes.getMonth()], " ", mes.getFullYear()), seta(1)), /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'grid',
        gridTemplateColumns: 'repeat(7,minmax(0,1fr))',
        gap: 2
      }
    }, SEMANA.map((s, i) => /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        textAlign: 'center',
        fontSize: 11,
        fontWeight: 600,
        color: 'var(--cor-texto-suave)',
        paddingBottom: 4
      }
    }, s)), celulas.map((d, i) => {
      if (!d) return /*#__PURE__*/React.createElement("div", {
        key: i
      });
      const dentro = fimVisivel && inicio && dia(d) > dia(inicio) && dia(d) < dia(fimVisivel);
      const ponta = mesmoDia(d, inicio) || mesmoDia(d, fimVisivel) || modo === 'dia' && mesmoDia(d, valor);
      const hoje = mesmoDia(d, new Date());
      const off = bloqueado(d);
      return /*#__PURE__*/React.createElement("button", {
        key: i,
        type: "button",
        disabled: off,
        onClick: () => clicar(d),
        onMouseEnter: () => setPairando(d),
        onMouseLeave: () => setPairando(null),
        "aria-current": hoje ? 'date' : undefined,
        style: {
          font: 'inherit',
          fontSize: 13,
          fontVariantNumeric: 'tabular-nums',
          padding: '8px 0',
          minWidth: 0,
          border: 'none',
          borderRadius: 'var(--raio-pequeno)',
          cursor: off ? 'not-allowed' : 'pointer',
          opacity: off ? 0.3 : 1,
          background: ponta ? 'var(--cor-acao)' : dentro ? 'var(--cor-superficie-2)' : 'transparent',
          color: ponta ? 'var(--cor-texto-invertido)' : 'var(--cor-texto)',
          fontWeight: ponta || hoje ? 600 : 400,
          boxShadow: hoje && !ponta ? 'inset 0 0 0 1px var(--cor-borda-controle)' : 'none',
          transition: 'background 120ms ease'
        }
      }, d.getDate());
    })))
  );
}
Object.assign(__ds_scope, { Calendario });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/Calendario.jsx", error: String((e && e.message) || e) }); }

// components/formulario/Campo.jsx
try { (() => {
/* global React */
const {
  useState
} = React;
function Envoltorio({
  rotulo,
  obrigatorio,
  dica,
  erro,
  children
}) {
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      fontFamily: 'var(--fonte-ui)'
    }
  }, rotulo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-texto)'
    }
  }, rotulo, obrigatorio ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--cor-destaque)',
      marginLeft: 2
    }
  }, "*") : null) : null, children, erro ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-erro)',
      fontWeight: 600
    }
  }, erro) : dica ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)'
    }
  }, dica) : null);
}
function entradaEstilo({
  foco,
  erro,
  desabilitado
}) {
  return {
    fontFamily: 'var(--fonte-ui)',
    fontSize: 15,
    color: 'var(--cor-texto)',
    padding: '9px 12px',
    width: '100%',
    boxSizing: 'border-box',
    border: '1px solid ' + (erro ? 'var(--cor-erro)' : foco ? 'var(--cor-acao)' : 'var(--cor-borda-forte)'),
    borderRadius: 'var(--raio-pequeno)',
    background: desabilitado ? 'var(--cor-superficie-2)' : 'var(--cor-superficie)',
    opacity: desabilitado ? 0.55 : 1,
    outline: 'none',
    boxShadow: foco ? erro ? 'var(--anel-foco-erro)' : 'var(--anel-foco)' : 'none',
    transition: 'border-color 150ms ease, box-shadow 150ms ease'
  };
}
function Campo({
  rotulo,
  valor,
  aoMudar,
  placeholder,
  dica,
  erro,
  tipo = 'text',
  obrigatorio,
  desabilitado,
  prefixo,
  sufixo,
  style
}) {
  const [foco, setFoco] = useState(false);
  const entrada = /*#__PURE__*/React.createElement("input", {
    type: tipo,
    value: valor ?? '',
    placeholder: placeholder,
    disabled: desabilitado,
    onChange: e => aoMudar && aoMudar(e.target.value),
    onFocus: () => setFoco(true),
    onBlur: () => setFoco(false),
    style: {
      ...entradaEstilo({
        foco,
        erro,
        desabilitado
      }),
      border: prefixo || sufixo ? 'none' : entradaEstilo({
        foco,
        erro,
        desabilitado
      }).border,
      boxShadow: prefixo || sufixo ? 'none' : entradaEstilo({
        foco,
        erro,
        desabilitado
      }).boxShadow,
      background: 'transparent',
      paddingLeft: prefixo ? 0 : 12,
      paddingRight: sufixo ? 0 : 12
    }
  });
  return /*#__PURE__*/React.createElement(Envoltorio, {
    rotulo: rotulo,
    obrigatorio: obrigatorio,
    dica: dica,
    erro: erro
  }, prefixo || sufixo ? /*#__PURE__*/React.createElement("span", {
    style: {
      ...entradaEstilo({
        foco,
        erro,
        desabilitado
      }),
      display: 'flex',
      alignItems: 'center',
      gap: 8,
      padding: '0 12px',
      ...style
    }
  }, prefixo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14,
      color: 'var(--cor-texto-suave)',
      flex: 'none'
    }
  }, prefixo) : null, entrada, sufixo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14,
      color: 'var(--cor-texto-suave)',
      flex: 'none'
    }
  }, sufixo) : null) : React.cloneElement(entrada, {
    style: {
      ...entradaEstilo({
        foco,
        erro,
        desabilitado
      }),
      ...style
    }
  }));
}
Object.assign(__ds_scope, { Campo });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/Campo.jsx", error: String((e && e.message) || e) }); }

// components/formulario/Escolha.jsx
try { (() => {
/* global React */
function Escolha({
  tipo = 'caixa',
  rotulo,
  descricao,
  marcado,
  aoMudar,
  nome,
  valor,
  desabilitado,
  style
}) {
  const redondo = tipo === 'radio';
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'flex-start',
      gap: 8,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      color: 'var(--cor-texto)',
      cursor: desabilitado ? 'not-allowed' : 'pointer',
      opacity: desabilitado ? 0.45 : 1,
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 18,
      height: 18,
      marginTop: 2,
      flex: 'none',
      boxSizing: 'border-box',
      borderRadius: redondo ? 999 : 'var(--raio-pequeno)',
      border: '1px solid ' + (marcado ? 'var(--cor-acao)' : 'var(--cor-borda-forte)'),
      background: marcado ? 'var(--cor-acao)' : 'var(--cor-superficie)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      transition: 'background 150ms ease, border-color 150ms ease'
    }
  }, marcado && !redondo ? /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 16 16",
    fill: "none",
    stroke: "var(--cor-texto-invertido)",
    strokeWidth: "2.4",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M3.5 8.5l3 3 6-6"
  })) : null, marcado && redondo ? /*#__PURE__*/React.createElement("span", {
    style: {
      width: 7,
      height: 7,
      borderRadius: 999,
      background: 'var(--cor-texto-invertido)'
    }
  }) : null), /*#__PURE__*/React.createElement("input", {
    type: redondo ? 'radio' : 'checkbox',
    name: nome,
    value: valor,
    checked: !!marcado,
    disabled: desabilitado,
    onChange: e => aoMudar && aoMudar(e.target.checked),
    style: {
      position: 'absolute',
      opacity: 0,
      width: 0,
      height: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", null, rotulo), descricao ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--cor-texto-suave)',
      lineHeight: 1.45
    }
  }, descricao) : null));
}
Object.assign(__ds_scope, { Escolha });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/Escolha.jsx", error: String((e && e.message) || e) }); }

// components/formulario/Interruptor.jsx
try { (() => {
/* global React */
function Interruptor({
  rotulo,
  descricao,
  ligado,
  aoMudar,
  desabilitado,
  style
}) {
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'flex-start',
      gap: 12,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      color: 'var(--cor-texto)',
      cursor: desabilitado ? 'not-allowed' : 'pointer',
      opacity: desabilitado ? 0.45 : 1,
      ...style
    }
  }, /*#__PURE__*/React.createElement("span", {
    role: "switch",
    "aria-checked": !!ligado,
    tabIndex: desabilitado ? -1 : 0,
    onClick: () => !desabilitado && aoMudar && aoMudar(!ligado),
    onKeyDown: e => {
      if ((e.key === ' ' || e.key === 'Enter') && !desabilitado) {
        e.preventDefault();
        aoMudar && aoMudar(!ligado);
      }
    },
    style: {
      width: 38,
      height: 22,
      borderRadius: 999,
      flex: 'none',
      marginTop: 1,
      position: 'relative',
      background: ligado ? 'var(--cor-acao)' : 'var(--cor-borda-controle)',
      transition: 'background 150ms ease',
      outline: 'none'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: 'absolute',
      top: 3,
      left: 3,
      width: 16,
      height: 16,
      borderRadius: 999,
      background: 'var(--cor-superficie)',
      boxShadow: 'var(--sombra-pequena)',
      transform: ligado ? 'translateX(16px)' : 'none',
      transition: 'transform 150ms ease'
    }
  })), rotulo || descricao ? /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", null, rotulo), descricao ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--cor-texto-suave)',
      lineHeight: 1.45
    }
  }, descricao) : null) : null);
}
Object.assign(__ds_scope, { Interruptor });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/Interruptor.jsx", error: String((e && e.message) || e) }); }

// components/formulario/MultiSelecao.jsx
try { (() => {
/* global React */
const {
  useState,
  useRef,
  useEffect,
  useMemo
} = React;
function normaliza(o) {
  return typeof o === 'string' ? {
    valor: o,
    rotulo: o
  } : o;
}

/* Multi-seleção: fichas do que já foi escolhido + lista com caixas de marcar.
   Acima de ~8 opções a busca aparece sozinha; abaixo dela seria só ruído. */
function MultiSelecao({
  rotulo,
  opcoes = [],
  valor = [],
  aoMudar,
  placeholder = 'Selecionar…',
  dica,
  erro,
  obrigatorio,
  desabilitado,
  limite,
  maxFichas = 3,
  style
}) {
  const [aberto, setAberto] = useState(false);
  const [busca, setBusca] = useState('');
  const [foco, setFoco] = useState(false);
  const raiz = useRef(null);
  const itens = useMemo(() => opcoes.map(normaliza), [opcoes]);
  const comBusca = itens.length > 8;
  const filtrados = useMemo(() => {
    const q = busca.trim().toLowerCase();
    return q ? itens.filter(o => o.rotulo.toLowerCase().includes(q)) : itens;
  }, [itens, busca]);
  useEffect(() => {
    if (!aberto) return;
    const fora = e => {
      if (raiz.current && !raiz.current.contains(e.target)) setAberto(false);
    };
    const tecla = e => {
      if (e.key === 'Escape') setAberto(false);
    };
    document.addEventListener('mousedown', fora);
    window.addEventListener('keydown', tecla);
    return () => {
      document.removeEventListener('mousedown', fora);
      window.removeEventListener('keydown', tecla);
    };
  }, [aberto]);
  const cheio = limite != null && valor.length >= limite;
  const alterna = v => {
    if (!aoMudar) return;
    if (valor.includes(v)) aoMudar(valor.filter(x => x !== v));else if (!cheio) aoMudar([...valor, v]);
  };
  const escolhidos = itens.filter(o => valor.includes(o.valor));
  const visiveis = escolhidos.slice(0, maxFichas);
  const restantes = escolhidos.length - visiveis.length;
  return /*#__PURE__*/React.createElement("div", {
    ref: raiz,
    style: {
      position: 'relative',
      fontFamily: 'var(--fonte-ui)',
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      ...style
    }
  }, rotulo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-texto)'
    }
  }, rotulo, obrigatorio ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--cor-destaque-texto)',
      marginLeft: 2
    }
  }, "*") : null) : null, /*#__PURE__*/React.createElement("button", {
    type: "button",
    disabled: desabilitado,
    onClick: () => setAberto(a => !a),
    onFocus: () => setFoco(true),
    onBlur: () => setFoco(false),
    "aria-expanded": aberto,
    "aria-haspopup": "listbox",
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      flexWrap: 'wrap',
      textAlign: 'left',
      minHeight: 40,
      width: '100%',
      boxSizing: 'border-box',
      padding: '6px 34px 6px 10px',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      color: 'var(--cor-texto)',
      background: desabilitado ? 'var(--cor-superficie-2)' : 'var(--cor-superficie)',
      border: '1px solid ' + (erro ? 'var(--cor-erro)' : foco || aberto ? 'var(--cor-acao)' : 'var(--cor-borda-controle)'),
      borderRadius: 'var(--raio-pequeno)',
      outline: 'none',
      boxShadow: foco || aberto ? erro ? 'var(--anel-foco-erro)' : 'var(--anel-foco)' : 'none',
      opacity: desabilitado ? 0.55 : 1,
      cursor: desabilitado ? 'not-allowed' : 'pointer',
      transition: 'border-color 150ms ease, box-shadow 150ms ease',
      backgroundImage: 'linear-gradient(45deg, transparent 50%, var(--cor-texto-suave) 50%), linear-gradient(135deg, var(--cor-texto-suave) 50%, transparent 50%)',
      backgroundPosition: 'calc(100% - 16px) 50%, calc(100% - 11px) 50%',
      backgroundSize: '5px 5px, 5px 5px',
      backgroundRepeat: 'no-repeat'
    }
  }, escolhidos.length === 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--cor-texto-suave)'
    }
  }, placeholder) : /*#__PURE__*/React.createElement(React.Fragment, null, visiveis.map(o => /*#__PURE__*/React.createElement("span", {
    key: o.valor,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      padding: '2px 4px 2px 8px',
      borderRadius: 999,
      background: 'var(--cor-acao-suave)',
      color: 'var(--cor-acao)',
      fontSize: 13,
      fontWeight: 600,
      lineHeight: 1.6
    }
  }, o.rotulo, /*#__PURE__*/React.createElement("span", {
    role: "button",
    "aria-label": 'Remover ' + o.rotulo,
    tabIndex: -1,
    onClick: e => {
      e.stopPropagation();
      alterna(o.valor);
    },
    style: {
      cursor: 'pointer',
      fontSize: 14,
      lineHeight: 1,
      padding: '0 3px',
      opacity: 0.75
    }
  }, "\xD7"))), restantes > 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      color: 'var(--cor-texto-suave)'
    }
  }, "+", restantes) : null)), aberto ? /*#__PURE__*/React.createElement("div", {
    role: "listbox",
    "aria-multiselectable": "true",
    style: {
      position: 'absolute',
      top: '100%',
      left: 0,
      right: 0,
      marginTop: 4,
      zIndex: 'var(--z-suspenso)',
      background: 'var(--cor-camada)',
      border: '1px solid var(--cor-borda-camada)',
      borderRadius: 'var(--raio-medio)',
      boxShadow: 'var(--sombra-media)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      padding: 6,
      maxHeight: 260,
      overflow: 'auto'
    }
  }, comBusca ? /*#__PURE__*/React.createElement("input", {
    autoFocus: true,
    value: busca,
    onChange: e => setBusca(e.target.value),
    placeholder: "Filtrar op\xE7\xF5es",
    "aria-label": "Filtrar op\xE7\xF5es",
    style: {
      width: '100%',
      boxSizing: 'border-box',
      marginBottom: 6,
      padding: '7px 10px',
      fontFamily: 'var(--fonte-ui)',
      fontSize: 14,
      color: 'var(--cor-texto)',
      background: 'var(--cor-superficie-2)',
      border: '1px solid var(--cor-borda)',
      borderRadius: 'var(--raio-pequeno)',
      outline: 'none'
    }
  }) : null, filtrados.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '10px 10px 12px',
      fontSize: 13,
      color: 'var(--cor-texto-suave)'
    }
  }, "Nada com esse nome. Tente menos letras.") : filtrados.map(o => {
    const marcado = valor.includes(o.valor);
    const bloqueado = !marcado && cheio;
    return /*#__PURE__*/React.createElement("div", {
      key: o.valor,
      role: "option",
      "aria-selected": marcado,
      onClick: () => !bloqueado && alterna(o.valor),
      style: {
        display: 'flex',
        alignItems: 'center',
        gap: 10,
        padding: '9px 10px',
        borderRadius: 'var(--raio-pequeno)',
        fontSize: 14,
        minHeight: 20,
        color: bloqueado ? 'var(--cor-texto-suave)' : 'var(--cor-texto)',
        background: marcado ? 'var(--cor-acao-suave)' : 'transparent',
        cursor: bloqueado ? 'not-allowed' : 'pointer',
        opacity: bloqueado ? 0.6 : 1
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        width: 16,
        height: 16,
        flex: 'none',
        borderRadius: 4,
        border: '1px solid ' + (marcado ? 'var(--cor-acao)' : 'var(--cor-borda-controle)'),
        background: marcado ? 'var(--cor-acao)' : 'var(--cor-superficie)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'var(--cor-texto-invertido)',
        fontSize: 11,
        lineHeight: 1,
        fontWeight: 700
      }
    }, marcado ? '✓' : ''), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1
      }
    }, o.rotulo));
  })) : null, erro ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-erro)',
      fontWeight: 600
    }
  }, erro) : /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)'
    }
  }, cheio ? 'Máximo de ' + limite + ' — desmarque uma para trocar.' : dica));
}
Object.assign(__ds_scope, { MultiSelecao });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/MultiSelecao.jsx", error: String((e && e.message) || e) }); }

// components/formulario/Selecao.jsx
try { (() => {
/* global React */
const {
  useState
} = React;
function Envoltorio({
  rotulo,
  obrigatorio,
  dica,
  erro,
  children
}) {
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      fontFamily: 'var(--fonte-ui)'
    }
  }, rotulo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-texto)'
    }
  }, rotulo, obrigatorio ? /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--cor-destaque)',
      marginLeft: 2
    }
  }, "*") : null) : null, children, erro ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-erro)',
      fontWeight: 600
    }
  }, erro) : dica ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)'
    }
  }, dica) : null);
}
function entradaEstilo({
  foco,
  erro,
  desabilitado
}) {
  return {
    fontFamily: 'var(--fonte-ui)',
    fontSize: 15,
    color: 'var(--cor-texto)',
    padding: '9px 12px',
    width: '100%',
    boxSizing: 'border-box',
    border: '1px solid ' + (erro ? 'var(--cor-erro)' : foco ? 'var(--cor-acao)' : 'var(--cor-borda-forte)'),
    borderRadius: 'var(--raio-pequeno)',
    background: desabilitado ? 'var(--cor-superficie-2)' : 'var(--cor-superficie)',
    opacity: desabilitado ? 0.55 : 1,
    outline: 'none',
    boxShadow: foco ? erro ? 'var(--anel-foco-erro)' : 'var(--anel-foco)' : 'none',
    transition: 'border-color 150ms ease, box-shadow 150ms ease'
  };
}
function Selecao({
  rotulo,
  valor,
  aoMudar,
  opcoes = [],
  placeholder,
  dica,
  erro,
  obrigatorio,
  desabilitado,
  style
}) {
  const [foco, setFoco] = useState(false);
  return /*#__PURE__*/React.createElement(Envoltorio, {
    rotulo: rotulo,
    obrigatorio: obrigatorio,
    dica: dica,
    erro: erro
  }, /*#__PURE__*/React.createElement("select", {
    value: valor ?? '',
    disabled: desabilitado,
    onChange: e => aoMudar && aoMudar(e.target.value),
    onFocus: () => setFoco(true),
    onBlur: () => setFoco(false),
    style: {
      ...entradaEstilo({
        foco,
        erro,
        desabilitado
      }),
      appearance: 'none',
      paddingRight: 34,
      cursor: desabilitado ? 'not-allowed' : 'pointer',
      backgroundImage: 'linear-gradient(45deg, transparent 50%, var(--cor-texto-suave) 50%), linear-gradient(135deg, var(--cor-texto-suave) 50%, transparent 50%)',
      backgroundPosition: 'calc(100% - 16px) 50%, calc(100% - 11px) 50%',
      backgroundSize: '5px 5px, 5px 5px',
      backgroundRepeat: 'no-repeat',
      ...style
    }
  }, placeholder ? /*#__PURE__*/React.createElement("option", {
    value: "",
    disabled: true
  }, placeholder) : null, opcoes.map(o => {
    const v = typeof o === 'string' ? o : o.valor;
    const t = typeof o === 'string' ? o : o.rotulo;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, t);
  })));
}
Object.assign(__ds_scope, { Selecao });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/Selecao.jsx", error: String((e && e.message) || e) }); }

// components/formulario/SeletorPeriodo.jsx
try { (() => {
/* global React */
const {
  useState,
  useRef,
  useEffect
} = React;
const d0 = d => new Date(d.getFullYear(), d.getMonth(), d.getDate());
const somaDias = (d, n) => {
  const x = d0(d);
  x.setDate(x.getDate() + n);
  return x;
};
const fmt = d => d.toLocaleDateString('pt-BR', {
  day: '2-digit',
  month: 'short'
}).replace('.', '');
const fmtLongo = d => d.toLocaleDateString('pt-BR', {
  day: '2-digit',
  month: 'short',
  year: 'numeric'
}).replace('.', '');

/* Atalhos resolvem quase todo o uso em dashboard. O calendário é o
   escape, não o caminho principal — por isso ele é o último chip e
   abre em popover, não ocupando espaço permanente. */
const ATALHOS = [{
  chave: 'hoje',
  rotulo: 'Hoje',
  dias: 0
}, {
  chave: '7d',
  rotulo: '7 dias',
  dias: 6
}, {
  chave: '30d',
  rotulo: '30 dias',
  dias: 29
}, {
  chave: 'mes',
  rotulo: 'Este mês',
  mes: true
}, {
  chave: 'ano',
  rotulo: 'Este ano',
  ano: true
}];
function periodoDoAtalho(chave, hoje = new Date()) {
  const a = ATALHOS.find(x => x.chave === chave);
  if (!a) return null;
  const fim = d0(hoje);
  if (a.mes) return {
    inicio: new Date(fim.getFullYear(), fim.getMonth(), 1),
    fim
  };
  if (a.ano) return {
    inicio: new Date(fim.getFullYear(), 0, 1),
    fim
  };
  return {
    inicio: somaDias(fim, -a.dias),
    fim
  };
}

/* O período anterior de mesma duração, colado no início do atual.
   É sempre isso que se quer comparar: "os 30 dias antes destes 30". */
function periodoAnterior({
  inicio,
  fim
}) {
  const dias = Math.round((d0(fim) - d0(inicio)) / 86400000) + 1;
  return {
    inicio: somaDias(inicio, -dias),
    fim: somaDias(inicio, -1)
  };
}
function SeletorPeriodo({
  valor,
  aoMudar,
  comparavel = true,
  atalhos = ATALHOS,
  hoje = new Date(),
  style
}) {
  const [aberto, setAberto] = useState(false);
  const [rascunho, setRascunho] = useState(null);
  const caixa = useRef(null);
  useEffect(() => {
    if (!aberto) return;
    const fora = e => {
      if (caixa.current && !caixa.current.contains(e.target)) setAberto(false);
    };
    const esc = e => {
      if (e.key === 'Escape') setAberto(false);
    };
    document.addEventListener('mousedown', fora);
    document.addEventListener('keydown', esc);
    return () => {
      document.removeEventListener('mousedown', fora);
      document.removeEventListener('keydown', esc);
    };
  }, [aberto]);
  const atual = valor || {
    chave: '30d',
    ...periodoDoAtalho('30d', hoje),
    comparar: false
  };
  const custom = atual.chave === 'custom';
  function escolher(patch) {
    const novo = {
      ...atual,
      ...patch
    };
    novo.comparacao = novo.comparar ? periodoAnterior(novo) : null;
    aoMudar && aoMudar(novo);
  }
  const chip = ativo => ({
    font: 'inherit',
    fontSize: 13,
    fontWeight: 600,
    cursor: 'pointer',
    whiteSpace: 'nowrap',
    padding: '7px 14px',
    borderRadius: 'var(--raio-pilula)',
    transition: 'background 120ms ease',
    border: '1px solid ' + (ativo ? 'var(--cor-acao)' : 'var(--cor-borda)'),
    background: ativo ? 'var(--cor-acao)' : 'var(--cor-superficie)',
    color: ativo ? 'var(--cor-texto-invertido)' : 'var(--cor-texto)'
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      display: 'flex',
      flexWrap: 'wrap',
      alignItems: 'center',
      gap: 'var(--espaco-2)',
      ...style
    }
  }, atalhos.map(a => /*#__PURE__*/React.createElement("button", {
    key: a.chave,
    type: "button",
    "aria-pressed": atual.chave === a.chave,
    onClick: () => escolher({
      chave: a.chave,
      ...periodoDoAtalho(a.chave, hoje)
    }),
    style: chip(atual.chave === a.chave)
  }, a.rotulo)), /*#__PURE__*/React.createElement("div", {
    ref: caixa,
    style: {
      position: 'relative'
    }
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    "aria-expanded": aberto,
    "aria-haspopup": "dialog",
    onClick: () => {
      setRascunho(custom ? {
        inicio: atual.inicio,
        fim: atual.fim
      } : {
        inicio: null,
        fim: null
      });
      setAberto(v => !v);
    },
    style: chip(custom)
  }, custom ? fmt(atual.inicio) + ' \u2013 ' + fmt(atual.fim) : 'Escolher datas'), aberto ? /*#__PURE__*/React.createElement("div", {
    role: "dialog",
    "aria-label": "Escolher intervalo de datas",
    style: {
      position: 'absolute',
      top: 'calc(100% + 8px)',
      left: 0,
      zIndex: 'var(--z-suspenso)',
      background: 'var(--cor-camada)',
      border: '1px solid var(--cor-borda-camada)',
      borderRadius: 'var(--raio-medio)',
      boxShadow: 'var(--sombra-grande)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      padding: 'var(--espaco-4)'
    }
  }, /*#__PURE__*/React.createElement(CalendarioSlot, {
    modo: "intervalo",
    valor: rascunho,
    aoSelecionar: setRascunho,
    max: d0(hoje)
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      gap: 'var(--espaco-3)',
      marginTop: 'var(--espaco-3)',
      paddingTop: 'var(--espaco-3)',
      borderTop: '1px solid var(--cor-borda)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--cor-texto-suave)'
    }
  }, rascunho?.inicio && rascunho?.fim ? fmtLongo(rascunho.inicio) + ' a ' + fmtLongo(rascunho.fim) : rascunho?.inicio ? 'Agora escolha o fim' : 'Escolha o início'), /*#__PURE__*/React.createElement("button", {
    type: "button",
    disabled: !rascunho?.inicio || !rascunho?.fim,
    onClick: () => {
      escolher({
        chave: 'custom',
        ...rascunho
      });
      setAberto(false);
    },
    style: {
      font: 'inherit',
      fontSize: 13,
      fontWeight: 600,
      padding: '7px 14px',
      borderRadius: 'var(--raio-pequeno)',
      border: '1px solid var(--cor-acao)',
      background: 'var(--cor-acao)',
      color: 'var(--cor-texto-invertido)',
      cursor: rascunho?.inicio && rascunho?.fim ? 'pointer' : 'not-allowed',
      opacity: rascunho?.inicio && rascunho?.fim ? 1 : 0.45
    }
  }, "Aplicar"))) : null), comparavel ? /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 8,
      cursor: 'pointer',
      fontSize: 13,
      color: 'var(--cor-texto)',
      marginLeft: 'var(--espaco-2)'
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: !!atual.comparar,
    onChange: e => escolher({
      comparar: e.target.checked
    }),
    style: {
      width: 16,
      height: 16,
      accentColor: 'var(--cor-acao)',
      cursor: 'pointer'
    }
  }), "Comparar com per\xEDodo anterior", atual.comparar ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--cor-texto-suave)'
    }
  }, "(", fmt(periodoAnterior(atual).inicio), " ", '\u2013', " ", fmt(periodoAnterior(atual).fim), ")") : null) : null);
}

/* Só nomes com inicial maiúscula entram no namespace global do bundle,
   então as duas funções auxiliares viajam penduradas no componente:
   SeletorPeriodo.periodoDoAtalho(...) e SeletorPeriodo.periodoAnterior(...). */
SeletorPeriodo.periodoDoAtalho = periodoDoAtalho;
SeletorPeriodo.periodoAnterior = periodoAnterior;
SeletorPeriodo.ATALHOS = ATALHOS;

/* Resolve o Calendario em tempo de render: assim o SeletorPeriodo
   funciona tanto importado como módulo quanto pelo bundle global. */
function CalendarioSlot(props) {
  const ns = typeof window !== 'undefined' ? Object.keys(window).find(k => k.startsWith('AADesignSystem')) : null;
  const Cal = ns && window[ns] && window[ns].Calendario || null;
  if (!Cal) return /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      color: 'var(--cor-texto-suave)',
      width: '100%',
      maxWidth: 266
    }
  }, "Calendario n\xE3o carregado.");
  return /*#__PURE__*/React.createElement(Cal, props);
}

/* Só nomes com maiúscula chegam ao namespace do bundle. As duas contas saem
   agrupadas: Periodo.doAtalho() e Periodo.anterior(). */
const Periodo = {
  doAtalho: periodoDoAtalho,
  anterior: periodoAnterior,
  atalhos: ATALHOS
};
Object.assign(__ds_scope, { ATALHOS, periodoDoAtalho, periodoAnterior, SeletorPeriodo, Periodo });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/formulario/SeletorPeriodo.jsx", error: String((e && e.message) || e) }); }

// components/graficos/GraficoBarras.jsx
try { (() => {
/* global React */
const NF = new Intl.NumberFormat('pt-BR');
const PALETA = ['var(--dados-1)', 'var(--dados-2)', 'var(--dados-3)', 'var(--dados-4)', 'var(--dados-5)', 'var(--dados-6)', 'var(--dados-7)', 'var(--dados-8)'];
function passoBonito(bruto) {
  const exp = Math.floor(Math.log10(bruto || 1));
  const base = Math.pow(10, exp);
  const n = (bruto || 1) / base;
  const m = n <= 1 ? 1 : n <= 2 ? 2 : n <= 2.5 ? 2.5 : n <= 5 ? 5 : 10;
  return m * base;
}
function marcas(max, min, divisoes) {
  const d = divisoes || 4;
  const passo = passoBonito((max - min) / d);
  const topo = Math.ceil(max / passo) * passo;
  const piso = min < 0 ? Math.floor(min / passo) * passo : 0;
  const out = [];
  for (let v = piso; v <= topo + passo * 0.001; v += passo) out.push(Number(v.toFixed(6)));
  return {
    marcas: out,
    topo,
    piso
  };
}
function Legenda({
  series,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 16,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 12,
      color: 'var(--cor-texto-suave)',
      ...style
    }
  }, series.map((s, i) => /*#__PURE__*/React.createElement("span", {
    key: s.chave || i,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 9,
      height: 9,
      borderRadius: 2,
      background: s.cor || PALETA[i % 8],
      flex: 'none'
    }
  }), s.nome || s.chave)));
}
function GraficoBarras({
  dados = [],
  series = [],
  empilhado,
  orientacao = 'vertical',
  altura = 240,
  mostrarGrade = true,
  mostrarLegenda = true,
  formatar,
  realce,
  style
}) {
  const fmt = formatar || (v => NF.format(v));
  const chaves = series.length ? series : [{
    chave: 'valor',
    nome: 'Valor'
  }];
  const W = 640,
    H = altura;
  const horizontal = orientacao === 'horizontal';
  const m = horizontal ? {
    t: 10,
    r: 16,
    b: 26,
    l: 108
  } : {
    t: 10,
    r: 12,
    b: 30,
    l: 46
  };
  const iw = W - m.l - m.r,
    ih = H - m.t - m.b;
  const totais = dados.map(d => empilhado ? chaves.reduce((s, k) => s + (Number(d[k.chave]) || 0), 0) : Math.max(...chaves.map(k => Number(d[k.chave]) || 0)));
  const {
    marcas: ticks,
    topo
  } = marcas(Math.max(1, ...totais), 0, 4);
  const passoBanda = (horizontal ? ih : iw) / (dados.length || 1);
  const larguraGrupo = passoBanda * 0.62;
  const larguraBarra = empilhado ? larguraGrupo : larguraGrupo / chaves.length;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: '0 0 ' + W + ' ' + H,
    style: {
      width: '100%',
      height: 'auto',
      display: 'block',
      overflow: 'visible'
    },
    role: "img"
  }, mostrarGrade ? ticks.map(t => {
    const p = t / topo;
    return horizontal ? /*#__PURE__*/React.createElement("line", {
      key: t,
      x1: m.l + p * iw,
      x2: m.l + p * iw,
      y1: m.t,
      y2: m.t + ih,
      stroke: "var(--grafico-grade)",
      strokeWidth: "1"
    }) : /*#__PURE__*/React.createElement("line", {
      key: t,
      x1: m.l,
      x2: m.l + iw,
      y1: m.t + ih - p * ih,
      y2: m.t + ih - p * ih,
      stroke: "var(--grafico-grade)",
      strokeWidth: "1"
    });
  }) : null, ticks.map(t => {
    const p = t / topo;
    return horizontal ? /*#__PURE__*/React.createElement("text", {
      key: 'l' + t,
      x: m.l + p * iw,
      y: m.t + ih + 17,
      textAnchor: "middle",
      fontSize: "10",
      fill: "var(--grafico-rotulo)",
      fontFamily: "var(--fonte-ui)"
    }, fmt(t)) : /*#__PURE__*/React.createElement("text", {
      key: 'l' + t,
      x: m.l - 8,
      y: m.t + ih - p * ih + 3.5,
      textAnchor: "end",
      fontSize: "10",
      fill: "var(--grafico-rotulo)",
      fontFamily: "var(--fonte-ui)"
    }, fmt(t));
  }), dados.map((d, i) => {
    const inicio = (horizontal ? m.t : m.l) + i * passoBanda + (passoBanda - larguraGrupo) / 2;
    let acumulado = 0;
    return /*#__PURE__*/React.createElement("g", {
      key: i
    }, chaves.map((k, j) => {
      const v = Number(d[k.chave]) || 0;
      const comp = v / topo * (horizontal ? iw : ih);
      const cor = realce && realce === d.rotulo ? 'var(--grafico-realce)' : k.cor || PALETA[j % 8];
      let x, y, w, h;
      if (horizontal) {
        x = m.l + (empilhado ? acumulado / topo * iw : 0);
        y = inicio + (empilhado ? 0 : j * larguraBarra);
        w = comp;
        h = Math.max(0, larguraBarra - (empilhado ? 0 : 2));
      } else {
        x = inicio + (empilhado ? 0 : j * larguraBarra);
        y = m.t + ih - comp - (empilhado ? acumulado / topo * ih : 0);
        w = Math.max(0, larguraBarra - (empilhado ? 0 : 2));
        h = comp;
      }
      acumulado += v;
      return /*#__PURE__*/React.createElement("rect", {
        key: k.chave,
        x: x,
        y: y,
        width: w,
        height: h,
        fill: cor,
        rx: "2"
      });
    }), horizontal ? /*#__PURE__*/React.createElement("text", {
      x: m.l - 10,
      y: inicio + larguraGrupo / 2 + 3.5,
      textAnchor: "end",
      fontSize: "11",
      fill: "var(--grafico-rotulo)",
      fontFamily: "var(--fonte-ui)"
    }, d.rotulo) : /*#__PURE__*/React.createElement("text", {
      x: inicio + larguraGrupo / 2,
      y: m.t + ih + 18,
      textAnchor: "middle",
      fontSize: "11",
      fill: "var(--grafico-rotulo)",
      fontFamily: "var(--fonte-ui)"
    }, d.rotulo));
  }), /*#__PURE__*/React.createElement("line", {
    x1: m.l,
    y1: m.t + ih,
    x2: m.l + iw,
    y2: m.t + ih,
    stroke: "var(--grafico-eixo)",
    strokeWidth: "1"
  })), mostrarLegenda && chaves.length > 1 ? /*#__PURE__*/React.createElement(Legenda, {
    series: chaves
  }) : null);
}
Object.assign(__ds_scope, { GraficoBarras });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/graficos/GraficoBarras.jsx", error: String((e && e.message) || e) }); }

// components/graficos/GraficoDispersao.jsx
try { (() => {
/* global React */
const NF = new Intl.NumberFormat('pt-BR');
const PALETA = ['var(--dados-1)', 'var(--dados-2)', 'var(--dados-3)', 'var(--dados-4)', 'var(--dados-5)', 'var(--dados-6)'];
function passoBonito(bruto) {
  const exp = Math.floor(Math.log10(bruto || 1)),
    base = Math.pow(10, exp),
    n = (bruto || 1) / base;
  return (n <= 1 ? 1 : n <= 2 ? 2 : n <= 2.5 ? 2.5 : n <= 5 ? 5 : 10) * base;
}
function eixo(max) {
  const passo = passoBonito(max / 4),
    topo = Math.ceil(max / passo) * passo,
    out = [];
  for (let v = 0; v <= topo + passo * 0.001; v += passo) out.push(Number(v.toFixed(6)));
  return {
    ticks: out,
    topo
  };
}
function GraficoDispersao({
  pontos = [],
  altura = 260,
  rotuloX,
  rotuloY,
  formatarX,
  formatarY,
  tamanhoBolha,
  style
}) {
  const fx = formatarX || (v => NF.format(v)),
    fy = formatarY || (v => NF.format(v));
  const W = 640,
    H = altura,
    m = {
      t: 12,
      r: 16,
      b: 38,
      l: 50
    };
  const iw = W - m.l - m.r,
    ih = H - m.t - m.b;
  const ex = eixo(Math.max(1, ...pontos.map(p => p.x)));
  const ey = eixo(Math.max(1, ...pontos.map(p => p.y)));
  const maxR = Math.max(1, ...pontos.map(p => p.tamanho || 1));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: '0 0 ' + W + ' ' + H,
    style: {
      width: '100%',
      height: 'auto',
      display: 'block',
      overflow: 'visible'
    },
    role: "img"
  }, ey.ticks.map(t => {
    const y = m.t + ih - t / ey.topo * ih;
    return /*#__PURE__*/React.createElement("g", {
      key: 'y' + t
    }, /*#__PURE__*/React.createElement("line", {
      x1: m.l,
      x2: m.l + iw,
      y1: y,
      y2: y,
      stroke: "var(--grafico-grade)",
      strokeWidth: "1"
    }), /*#__PURE__*/React.createElement("text", {
      x: m.l - 8,
      y: y + 3.5,
      textAnchor: "end",
      fontSize: "10",
      fill: "var(--grafico-rotulo)",
      fontFamily: "var(--fonte-ui)"
    }, fy(t)));
  }), ex.ticks.map(t => {
    const x = m.l + t / ex.topo * iw;
    return /*#__PURE__*/React.createElement("text", {
      key: 'x' + t,
      x: x,
      y: m.t + ih + 17,
      textAnchor: "middle",
      fontSize: "10",
      fill: "var(--grafico-rotulo)",
      fontFamily: "var(--fonte-ui)"
    }, fx(t));
  }), pontos.map((p, i) => /*#__PURE__*/React.createElement("circle", {
    key: i,
    cx: m.l + p.x / ex.topo * iw,
    cy: m.t + ih - p.y / ey.topo * ih,
    r: tamanhoBolha ? 4 + (p.tamanho || 1) / maxR * 14 : 4.5,
    fill: p.cor || PALETA[(p.grupo || 0) % 6],
    opacity: "0.72"
  })), /*#__PURE__*/React.createElement("line", {
    x1: m.l,
    y1: m.t + ih,
    x2: m.l + iw,
    y2: m.t + ih,
    stroke: "var(--grafico-eixo)",
    strokeWidth: "1"
  }), rotuloX ? /*#__PURE__*/React.createElement("text", {
    x: m.l + iw / 2,
    y: H - 4,
    textAnchor: "middle",
    fontSize: "10",
    letterSpacing: "0.06em",
    fill: "var(--grafico-rotulo)",
    fontFamily: "var(--fonte-ui)"
  }, rotuloX.toUpperCase()) : null, rotuloY ? /*#__PURE__*/React.createElement("text", {
    x: -(m.t + ih / 2),
    y: "12",
    transform: "rotate(-90)",
    textAnchor: "middle",
    fontSize: "10",
    letterSpacing: "0.06em",
    fill: "var(--grafico-rotulo)",
    fontFamily: "var(--fonte-ui)"
  }, rotuloY.toUpperCase()) : null));
}
Object.assign(__ds_scope, { GraficoDispersao });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/graficos/GraficoDispersao.jsx", error: String((e && e.message) || e) }); }

// components/graficos/GraficoFunil.jsx
try { (() => {
/* global React */
const NF = new Intl.NumberFormat('pt-BR');
const PALETA = ['var(--dados-1)', 'var(--dados-2)', 'var(--dados-3)', 'var(--dados-4)', 'var(--dados-5)', 'var(--dados-6)'];
function GraficoFunil({
  etapas = [],
  mostrarConversao = true,
  formatar,
  style
}) {
  const fmt = formatar || (v => NF.format(v));
  const topo = Math.max(1, ...etapas.map(e => e.valor));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, etapas.map((e, i) => {
    const p = e.valor / topo;
    const anterior = i ? etapas[i - 1].valor : null;
    return /*#__PURE__*/React.createElement("div", {
      key: e.rotulo,
      style: {
        display: 'flex',
        flexDirection: 'column',
        gap: 4
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: 'flex',
        justifyContent: 'space-between',
        fontSize: 13,
        color: 'var(--cor-texto)'
      }
    }, /*#__PURE__*/React.createElement("span", null, e.rotulo), /*#__PURE__*/React.createElement("span", {
      style: {
        display: 'flex',
        gap: 12,
        color: 'var(--cor-texto-suave)',
        fontVariantNumeric: 'tabular-nums'
      }
    }, mostrarConversao && anterior ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 11
      }
    }, Math.round(e.valor / anterior * 100), "%") : null, /*#__PURE__*/React.createElement("strong", {
      style: {
        color: 'var(--cor-texto-forte)',
        fontWeight: 600
      }
    }, fmt(e.valor)))), /*#__PURE__*/React.createElement("div", {
      style: {
        height: 26,
        background: 'var(--cor-superficie-2)',
        borderRadius: 'var(--raio-pequeno)',
        overflow: 'hidden'
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        height: '100%',
        width: p * 100 + '%',
        background: e.cor || PALETA[i % 6],
        borderRadius: 'var(--raio-pequeno)',
        transition: 'width 240ms ease'
      }
    })));
  }));
}
Object.assign(__ds_scope, { GraficoFunil });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/graficos/GraficoFunil.jsx", error: String((e && e.message) || e) }); }

// components/graficos/GraficoLinhas.jsx
try { (() => {
/* global React */
const NF = new Intl.NumberFormat('pt-BR');
const PALETA = ['var(--dados-1)', 'var(--dados-2)', 'var(--dados-3)', 'var(--dados-4)', 'var(--dados-5)', 'var(--dados-6)', 'var(--dados-7)', 'var(--dados-8)'];
function passoBonito(bruto) {
  const exp = Math.floor(Math.log10(bruto || 1));
  const base = Math.pow(10, exp);
  const n = (bruto || 1) / base;
  const m = n <= 1 ? 1 : n <= 2 ? 2 : n <= 2.5 ? 2.5 : n <= 5 ? 5 : 10;
  return m * base;
}
function marcas(max, min, divisoes) {
  const d = divisoes || 4;
  const passo = passoBonito((max - min) / d);
  const topo = Math.ceil(max / passo) * passo;
  const piso = min < 0 ? Math.floor(min / passo) * passo : 0;
  const out = [];
  for (let v = piso; v <= topo + passo * 0.001; v += passo) out.push(Number(v.toFixed(6)));
  return {
    marcas: out,
    topo,
    piso
  };
}
function Legenda({
  series,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 16,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 12,
      color: 'var(--cor-texto-suave)',
      ...style
    }
  }, series.map((s, i) => /*#__PURE__*/React.createElement("span", {
    key: s.chave || i,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 9,
      height: 9,
      borderRadius: 2,
      background: s.cor || PALETA[i % 8],
      flex: 'none'
    }
  }), s.nome || s.chave)));
}
function GraficoLinhas({
  dados = [],
  series = [],
  area,
  altura = 240,
  mostrarGrade = true,
  mostrarPontos,
  mostrarLegenda = true,
  formatar,
  referencia,
  style
}) {
  const fmt = formatar || (v => NF.format(v));
  const chaves = series.length ? series : [{
    chave: 'valor',
    nome: 'Valor'
  }];
  const W = 640,
    H = altura,
    m = {
      t: 12,
      r: 14,
      b: 30,
      l: 46
    };
  const iw = W - m.l - m.r,
    ih = H - m.t - m.b;
  const todos = dados.flatMap(d => chaves.map(k => Number(d[k.chave]) || 0));
  const {
    marcas: ticks,
    topo,
    piso
  } = marcas(Math.max(1, ...todos), Math.min(0, ...todos), 4);
  const span = topo - piso || 1;
  const px = i => m.l + (dados.length > 1 ? i / (dados.length - 1) * iw : iw / 2);
  const py = v => m.t + ih - (v - piso) / span * ih;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: '0 0 ' + W + ' ' + H,
    style: {
      width: '100%',
      height: 'auto',
      display: 'block',
      overflow: 'visible'
    },
    role: "img"
  }, mostrarGrade ? ticks.map(t => /*#__PURE__*/React.createElement("line", {
    key: t,
    x1: m.l,
    x2: m.l + iw,
    y1: py(t),
    y2: py(t),
    stroke: "var(--grafico-grade)",
    strokeWidth: "1"
  })) : null, ticks.map(t => /*#__PURE__*/React.createElement("text", {
    key: 'l' + t,
    x: m.l - 8,
    y: py(t) + 3.5,
    textAnchor: "end",
    fontSize: "10",
    fill: "var(--grafico-rotulo)",
    fontFamily: "var(--fonte-ui)"
  }, fmt(t))), referencia != null ? /*#__PURE__*/React.createElement("g", null, /*#__PURE__*/React.createElement("line", {
    x1: m.l,
    x2: m.l + iw,
    y1: py(referencia),
    y2: py(referencia),
    stroke: "var(--grafico-referencia)",
    strokeWidth: "1",
    strokeDasharray: "4 4"
  }), /*#__PURE__*/React.createElement("text", {
    x: m.l + iw,
    y: py(referencia) - 5,
    textAnchor: "end",
    fontSize: "10",
    fill: "var(--grafico-referencia)",
    fontFamily: "var(--fonte-ui)"
  }, "meta")) : null, chaves.map((k, j) => {
    const cor = k.cor || PALETA[j % 8];
    const pts = dados.map((d, i) => [px(i), py(Number(d[k.chave]) || 0)]);
    const linha = pts.map((p, i) => (i ? 'L' : 'M') + p[0].toFixed(1) + ' ' + p[1].toFixed(1)).join(' ');
    return /*#__PURE__*/React.createElement("g", {
      key: k.chave
    }, area ? /*#__PURE__*/React.createElement("path", {
      d: linha + ' L' + px(dados.length - 1).toFixed(1) + ' ' + py(piso).toFixed(1) + ' L' + px(0).toFixed(1) + ' ' + py(piso).toFixed(1) + ' Z',
      fill: j === 0 ? 'var(--grafico-area)' : cor,
      opacity: j === 0 ? 1 : 0.14
    }) : null, /*#__PURE__*/React.createElement("path", {
      d: linha,
      fill: "none",
      stroke: cor,
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round"
    }), mostrarPontos ? pts.map((p, i) => /*#__PURE__*/React.createElement("circle", {
      key: i,
      cx: p[0],
      cy: p[1],
      r: "3",
      fill: "var(--cor-superficie)",
      stroke: cor,
      strokeWidth: "2"
    })) : null);
  }), dados.map((d, i) => /*#__PURE__*/React.createElement("text", {
    key: i,
    x: px(i),
    y: m.t + ih + 18,
    textAnchor: "middle",
    fontSize: "11",
    fill: "var(--grafico-rotulo)",
    fontFamily: "var(--fonte-ui)"
  }, d.rotulo)), /*#__PURE__*/React.createElement("line", {
    x1: m.l,
    y1: m.t + ih,
    x2: m.l + iw,
    y2: m.t + ih,
    stroke: "var(--grafico-eixo)",
    strokeWidth: "1"
  })), mostrarLegenda && chaves.length > 1 ? /*#__PURE__*/React.createElement(Legenda, {
    series: chaves
  }) : null);
}
Object.assign(__ds_scope, { GraficoLinhas });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/graficos/GraficoLinhas.jsx", error: String((e && e.message) || e) }); }

// components/graficos/GraficoMedidor.jsx
try { (() => {
/* global React */
function GraficoMedidor({
  valor = 0,
  max = 100,
  rotulo,
  tamanho = 180,
  faixas,
  formatar,
  style
}) {
  const fmt = formatar || (v => String(Math.round(v)));
  const pct = Math.max(0, Math.min(1, valor / (max || 1)));
  const R = tamanho / 2,
    r = R - 14,
    cx = R,
    cy = R;
  const arco = (de, ate, cor, largura) => {
    const a0 = Math.PI * (1 + de),
      a1 = Math.PI * (1 + ate);
    const x0 = cx + r * Math.cos(a0),
      y0 = cy + r * Math.sin(a0);
    const x1 = cx + r * Math.cos(a1),
      y1 = cy + r * Math.sin(a1);
    return /*#__PURE__*/React.createElement("path", {
      d: 'M' + x0 + ' ' + y0 + ' A' + r + ' ' + r + ' 0 ' + (ate - de > 0.5 ? 1 : 0) + ' 1 ' + x1 + ' ' + y1,
      fill: "none",
      stroke: cor,
      strokeWidth: largura,
      strokeLinecap: "round"
    });
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'inline-flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 4,
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: tamanho,
    height: tamanho * 0.62,
    viewBox: '0 0 ' + tamanho + ' ' + tamanho * 0.62,
    role: "img"
  }, arco(0, 1, 'var(--cor-superficie-2)', 12), (faixas || []).map((f, i) => arco(f.de / max, f.ate / max, f.cor, 12)), arco(0, Math.max(0.001, pct), faixas ? 'var(--cor-texto-forte)' : 'var(--cor-acao)', faixas ? 5 : 12), /*#__PURE__*/React.createElement("text", {
    x: cx,
    y: cy - 4,
    textAnchor: "middle",
    fontFamily: "var(--fonte-display)",
    fontSize: tamanho * 0.19,
    fontWeight: "600",
    fill: "var(--cor-texto-forte)"
  }, fmt(valor))), rotulo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-suave)'
    }
  }, rotulo) : null);
}
Object.assign(__ds_scope, { GraficoMedidor });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/graficos/GraficoMedidor.jsx", error: String((e && e.message) || e) }); }

// components/graficos/GraficoRosca.jsx
try { (() => {
/* global React */
const NF = new Intl.NumberFormat('pt-BR');
const PALETA = ['var(--dados-1)', 'var(--dados-2)', 'var(--dados-3)', 'var(--dados-4)', 'var(--dados-5)', 'var(--dados-6)', 'var(--dados-7)', 'var(--dados-8)'];
function passoBonito(bruto) {
  const exp = Math.floor(Math.log10(bruto || 1));
  const base = Math.pow(10, exp);
  const n = (bruto || 1) / base;
  const m = n <= 1 ? 1 : n <= 2 ? 2 : n <= 2.5 ? 2.5 : n <= 5 ? 5 : 10;
  return m * base;
}
function marcas(max, min, divisoes) {
  const d = divisoes || 4;
  const passo = passoBonito((max - min) / d);
  const topo = Math.ceil(max / passo) * passo;
  const piso = min < 0 ? Math.floor(min / passo) * passo : 0;
  const out = [];
  for (let v = piso; v <= topo + passo * 0.001; v += passo) out.push(Number(v.toFixed(6)));
  return {
    marcas: out,
    topo,
    piso
  };
}
function Legenda({
  series,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 16,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 12,
      color: 'var(--cor-texto-suave)',
      ...style
    }
  }, series.map((s, i) => /*#__PURE__*/React.createElement("span", {
    key: s.chave || i,
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 9,
      height: 9,
      borderRadius: 2,
      background: s.cor || PALETA[i % 8],
      flex: 'none'
    }
  }), s.nome || s.chave)));
}
function GraficoRosca({
  dados = [],
  rosca = true,
  tamanho = 200,
  legenda = true,
  centroRotulo,
  centroValor,
  formatar,
  style
}) {
  const fmt = formatar || (v => NF.format(v));
  const total = dados.reduce((s, d) => s + (Number(d.valor) || 0), 0) || 1;
  const R = tamanho / 2,
    r = rosca ? R * 0.62 : 0;
  let ang = -Math.PI / 2;
  const fatias = dados.map((d, i) => {
    const frac = (Number(d.valor) || 0) / total;
    const a0 = ang,
      a1 = ang + frac * Math.PI * 2;
    ang = a1;
    const grande = a1 - a0 > Math.PI ? 1 : 0;
    const p = (raio, a) => [R + raio * Math.cos(a), R + raio * Math.sin(a)];
    const [x0, y0] = p(R - 1, a0),
      [x1, y1] = p(R - 1, a1);
    const [x2, y2] = p(r, a1),
      [x3, y3] = p(r, a0);
    const d1 = 'M' + x0 + ' ' + y0 + ' A' + (R - 1) + ' ' + (R - 1) + ' 0 ' + grande + ' 1 ' + x1 + ' ' + y1 + (r ? ' L' + x2 + ' ' + y2 + ' A' + r + ' ' + r + ' 0 ' + grande + ' 0 ' + x3 + ' ' + y3 + ' Z' : ' L' + R + ' ' + R + ' Z');
    return {
      d: d1,
      cor: d.cor || PALETA[i % 8],
      rotulo: d.rotulo,
      valor: d.valor,
      frac
    };
  });
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 24,
      flexWrap: 'wrap',
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: tamanho,
    height: tamanho,
    viewBox: '0 0 ' + tamanho + ' ' + tamanho,
    style: {
      flex: 'none'
    },
    role: "img"
  }, fatias.map((f, i) => /*#__PURE__*/React.createElement("path", {
    key: i,
    d: f.d,
    fill: f.cor,
    stroke: "var(--cor-superficie)",
    strokeWidth: "2"
  })), rosca && (centroValor || centroRotulo) ? /*#__PURE__*/React.createElement("g", null, /*#__PURE__*/React.createElement("text", {
    x: R,
    y: R - 2,
    textAnchor: "middle",
    fontFamily: "var(--fonte-display)",
    fontSize: tamanho * 0.17,
    fontWeight: "600",
    fill: "var(--cor-texto-forte)"
  }, centroValor), /*#__PURE__*/React.createElement("text", {
    x: R,
    y: R + 16,
    textAnchor: "middle",
    fontFamily: "var(--fonte-ui)",
    fontSize: "10",
    letterSpacing: "0.06em",
    fill: "var(--grafico-rotulo)"
  }, (centroRotulo || '').toUpperCase())) : null), legenda ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8,
      fontSize: 13,
      minWidth: 150
    }
  }, fatias.map((f, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 9,
      height: 9,
      borderRadius: 2,
      background: f.cor,
      flex: 'none'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      color: 'var(--cor-texto)'
    }
  }, f.rotulo), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--cor-texto-suave)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, fmt(f.valor)), /*#__PURE__*/React.createElement("span", {
    style: {
      color: 'var(--cor-texto-suave)',
      fontVariantNumeric: 'tabular-nums',
      width: 38,
      textAlign: 'right'
    }
  }, Math.round(f.frac * 100), "%")))) : null);
}
Object.assign(__ds_scope, { GraficoRosca });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/graficos/GraficoRosca.jsx", error: String((e && e.message) || e) }); }

// components/graficos/MapaDeCalor.jsx
try { (() => {
/* global React */
const ESCALA_SEQ = ['var(--seq-1)', 'var(--seq-2)', 'var(--seq-3)', 'var(--seq-4)', 'var(--seq-5)'];
function MapaDeCalor({
  linhas = [],
  colunas = [],
  valores = [],
  tamanhoCelula = 30,
  formatar,
  legenda = true,
  style
}) {
  const chatos = valores.flat();
  const max = Math.max(1, ...chatos);
  const cor = v => ESCALA_SEQ[Math.min(4, Math.floor(v / max * 5 - 0.0001) + (v === 0 ? 0 : 0))] || ESCALA_SEQ[0];
  const fmt = formatar || (v => String(v));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'auto repeat(' + colunas.length + ', ' + tamanhoCelula + 'px)',
      gap: 3,
      alignItems: 'center'
    }
  }, /*#__PURE__*/React.createElement("span", null), colunas.map(c => /*#__PURE__*/React.createElement("span", {
    key: c,
    style: {
      fontSize: 10,
      color: 'var(--grafico-rotulo)',
      textAlign: 'center'
    }
  }, c)), linhas.map((l, i) => /*#__PURE__*/React.createElement(React.Fragment, {
    key: l
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)',
      paddingRight: 8,
      textAlign: 'right',
      whiteSpace: 'nowrap'
    }
  }, l), colunas.map((c, j) => {
    const v = (valores[i] || [])[j] || 0;
    return /*#__PURE__*/React.createElement("span", {
      key: c,
      title: l + ' · ' + c + ': ' + fmt(v),
      style: {
        height: tamanhoCelula,
        borderRadius: 3,
        background: v === 0 ? 'var(--cor-superficie-2)' : cor(v)
      }
    });
  })))), legenda ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 6,
      fontSize: 11,
      color: 'var(--cor-texto-suave)'
    }
  }, /*#__PURE__*/React.createElement("span", null, "menos"), ESCALA_SEQ.map(c => /*#__PURE__*/React.createElement("span", {
    key: c,
    style: {
      width: 14,
      height: 10,
      borderRadius: 2,
      background: c
    }
  })), /*#__PURE__*/React.createElement("span", null, "mais")) : null);
}
Object.assign(__ds_scope, { MapaDeCalor });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/graficos/MapaDeCalor.jsx", error: String((e && e.message) || e) }); }

// components/graficos/Minigrafico.jsx
try { (() => {
/* global React */
function Minigrafico({
  dados = [],
  tipo = 'linha',
  cor = 'var(--dados-1)',
  largura = 120,
  altura = 32,
  realceUltimo,
  style
}) {
  if (!dados.length) return null;
  const min = Math.min(...dados, 0),
    max = Math.max(...dados),
    span = max - min || 1;
  const W = 100,
    H = 30;
  if (tipo === 'barra') {
    const p = W / dados.length;
    return /*#__PURE__*/React.createElement("svg", {
      viewBox: '0 0 ' + W + ' ' + H,
      preserveAspectRatio: "none",
      style: {
        width: largura,
        height: altura,
        display: 'block',
        ...style
      }
    }, dados.map((v, i) => {
      const h = (v - min) / span * (H - 2) || 0.5;
      return /*#__PURE__*/React.createElement("rect", {
        key: i,
        x: i * p,
        y: H - h,
        width: Math.max(0.6, p - p * 0.32),
        height: h,
        fill: realceUltimo && i === dados.length - 1 ? 'var(--grafico-realce)' : cor,
        opacity: realceUltimo && i < dados.length - 1 ? 0.5 : 1
      });
    }));
  }
  const pts = dados.map((v, i) => [i / (dados.length - 1) * W, H - (v - min) / span * (H - 3) - 1.5]);
  const d = pts.map((p, i) => (i ? 'L' : 'M') + p[0].toFixed(1) + ' ' + p[1].toFixed(1)).join(' ');
  return /*#__PURE__*/React.createElement("svg", {
    viewBox: '0 0 ' + W + ' ' + H,
    preserveAspectRatio: "none",
    style: {
      width: largura,
      height: altura,
      display: 'block',
      overflow: 'visible',
      ...style
    }
  }, /*#__PURE__*/React.createElement("path", {
    d: d,
    fill: "none",
    stroke: cor,
    strokeWidth: "1.6",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    vectorEffect: "non-scaling-stroke"
  }), realceUltimo ? /*#__PURE__*/React.createElement("circle", {
    cx: pts[pts.length - 1][0],
    cy: pts[pts.length - 1][1],
    r: "2",
    fill: "var(--grafico-realce)",
    vectorEffect: "non-scaling-stroke"
  }) : null);
}
Object.assign(__ds_scope, { Minigrafico });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/graficos/Minigrafico.jsx", error: String((e && e.message) || e) }); }

// components/navegacao/BarraLateral.jsx
try { (() => {
/* global React */
const {
  useState
} = React;
function BarraLateral({
  marca = 'AA',
  subtitulo,
  grupos = [],
  ativo,
  aoNavegar,
  rodape,
  largura = 232,
  style
}) {
  const [hover, setHover] = useState(null);
  return /*#__PURE__*/React.createElement("nav", {
    style: {
      width: largura,
      flex: 'none',
      boxSizing: 'border-box',
      minHeight: '100%',
      background: 'var(--cor-superficie)',
      borderRight: '1px solid var(--cor-borda)',
      padding: '24px 12px',
      display: 'flex',
      flexDirection: 'column',
      gap: 24,
      fontFamily: 'var(--fonte-ui)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 8px',
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-forte)'
    }
  }, marca), subtitulo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--cor-texto-suave)'
    }
  }, subtitulo) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 24,
      flex: 1
    }
  }, grupos.map((g, gi) => /*#__PURE__*/React.createElement("div", {
    key: g.rotulo || gi,
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, g.rotulo ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-suave)',
      padding: '0 8px 6px'
    }
  }, g.rotulo) : null, (g.itens || []).map(it => {
    const sel = it.id === ativo;
    const k = gi + ':' + it.id;
    return /*#__PURE__*/React.createElement("button", {
      key: it.id,
      onClick: () => aoNavegar && aoNavegar(it.id),
      onMouseEnter: () => setHover(k),
      onMouseLeave: () => setHover(null),
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        gap: 8,
        textAlign: 'left',
        border: 'none',
        cursor: 'pointer',
        width: '100%',
        padding: '8px 10px',
        borderRadius: 'var(--raio-pequeno)',
        fontFamily: 'inherit',
        fontSize: 14,
        fontWeight: sel ? 600 : 400,
        color: sel ? 'var(--cor-acao)' : 'var(--cor-texto)',
        background: sel ? 'var(--cor-acao-suave)' : hover === k ? 'var(--cor-linha-hover)' : 'transparent',
        transition: 'background 150ms ease'
      }
    }, /*#__PURE__*/React.createElement("span", null, it.rotulo), it.contagem != null ? /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 11,
        fontWeight: 600,
        color: 'var(--cor-texto-suave)',
        fontVariantNumeric: 'tabular-nums'
      }
    }, it.contagem) : null);
  })))), rodape ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '0 8px'
    }
  }, rodape) : null);
}
Object.assign(__ds_scope, { BarraLateral });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navegacao/BarraLateral.jsx", error: String((e && e.message) || e) }); }

// components/navegacao/BarraSuperior.jsx
try { (() => {
/* global React */
function BarraSuperior({
  rotulo,
  titulo,
  acoes,
  style
}) {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      alignItems: 'flex-end',
      justifyContent: 'space-between',
      gap: 24,
      flexWrap: 'wrap',
      minWidth: 0,
      padding: '24px 32px',
      borderBottom: '1px solid var(--cor-borda)',
      background: 'var(--cor-camada)',
      fontFamily: 'var(--fonte-ui)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      ...style
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4,
      flex: '1 1 240px',
      minWidth: 0
    }
  }, rotulo ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: 'var(--cor-texto-suave)',
      whiteSpace: 'nowrap'
    }
  }, rotulo) : null, titulo ? /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: 'var(--fonte-display)',
      fontSize: 36,
      fontWeight: 600,
      lineHeight: 1.1,
      letterSpacing: '-0.01em',
      color: 'var(--cor-texto-forte)',
      textWrap: 'balance'
    }
  }, titulo) : null), acoes ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      flexWrap: 'wrap',
      flex: '0 1 auto'
    }
  }, acoes) : null);
}
Object.assign(__ds_scope, { BarraSuperior });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navegacao/BarraSuperior.jsx", error: String((e && e.message) || e) }); }

// ui_kits/casa-projetos/App.jsx
try { (() => {
/* global React, Sidebar, TopBar, MetricCard, BarChart, ProjectRow, ProjectDetail, NewProjectModal, Alert, Eyebrow, Button */
const {
  useState,
  useMemo
} = React;
const INITIAL_PROJECTS = [{
  id: 'p1',
  name: 'Reforma da cozinha',
  subtitle: 'armários, bancada e elétrica',
  status: 'andamento',
  updated: 'há 2 dias',
  notes: 'Trocar a bancada por uma de madeira maciça, refazer a parte elétrica dos tomadas baixas, manter os armários superiores. O orçamento aprovado é o do João.',
  tasks: [{
    id: 't1',
    label: 'Confirmar medidas com o marceneiro',
    done: true
  }, {
    id: 't2',
    label: 'Comprar tomadas e disjuntores',
    done: true
  }, {
    id: 't3',
    label: 'Combinar dia da entrega da bancada',
    done: false
  }, {
    id: 't4',
    label: 'Pintar a parede atrás da pia',
    done: false
  }, {
    id: 't5',
    label: 'Fotografar tudo "antes" para o álbum',
    done: false
  }]
}, {
  id: 'p2',
  name: 'Pomar dos fundos',
  subtitle: 'jabuticaba, pitanga e amora',
  status: 'pendente',
  updated: 'há 5 dias',
  notes: 'Pesquisar viveiros de Atibaia que vendem mudas adultas. A jabuticaba é prioridade — quero plantar na próxima chuva.',
  tasks: [{
    id: 't1',
    label: 'Pedir orçamento de 3 viveiros',
    done: true
  }, {
    id: 't2',
    label: 'Marcar visita no Sítio das Mudas',
    done: false
  }, {
    id: 't3',
    label: 'Limpar a área dos fundos',
    done: false
  }]
}, {
  id: 'p3',
  name: 'Biblioteca da sala',
  subtitle: 'estante de canto · 4 prateleiras',
  status: 'rascunho',
  updated: 'há 11 dias',
  notes: 'Ainda em estudo. Talvez modular com nichos de tamanhos variados. Olhar referências antes de fechar o desenho.',
  tasks: [{
    id: 't1',
    label: 'Salvar 5 referências no quadro',
    done: false
  }, {
    id: 't2',
    label: 'Medir parede de canto',
    done: false
  }]
}, {
  id: 'p4',
  name: 'Hortinha de temperos',
  subtitle: 'janela da cozinha · 6 vasos',
  status: 'concluido',
  updated: 'há 1 mês',
  notes: 'Manjericão, alecrim, salsinha, cebolinha, hortelã e tomilho. Todos prosperando — a hortelã já precisa de poda.',
  tasks: [{
    id: 't1',
    label: 'Comprar vasos e terra',
    done: true
  }, {
    id: 't2',
    label: 'Plantar mudas',
    done: true
  }, {
    id: 't3',
    label: 'Pendurar o suporte na janela',
    done: true
  }]
}, {
  id: 'p5',
  name: 'Reorganizar o escritório',
  subtitle: 'cabos, papéis e a estante',
  status: 'atrasado',
  updated: 'há 3 dias',
  notes: 'Era para ter terminado no mês passado. Maior parte do trabalho é descartar o que não uso há mais de um ano.',
  tasks: [{
    id: 't1',
    label: 'Triagem das gavetas',
    done: true
  }, {
    id: 't2',
    label: 'Organizar cabos com velcro',
    done: false
  }, {
    id: 't3',
    label: 'Descartar caixas antigas',
    done: false
  }, {
    id: 't4',
    label: 'Reposicionar a impressora',
    done: false
  }]
}];
const MONTHS = ['Janeiro', 'Fevereiro', 'Março', 'Abril', 'Maio', 'Junho', 'Julho', 'Agosto', 'Setembro', 'Outubro', 'Novembro', 'Dezembro'];
function App() {
  const [section, setSection] = useState('resumo');
  const [projects, setProjects] = useState(INITIAL_PROJECTS);
  const [openProjectId, setOpenProjectId] = useState(null);
  const [showNew, setShowNew] = useState(false);
  const [monthIdx, setMonthIdx] = useState(4); // Maio (May), like the design guide's domestic feel
  const [year, setYear] = useState(2026);
  const [savedFlash, setSavedFlash] = useState(false);
  const openProject = projects.find(p => p.id === openProjectId) || null;

  // Metrics derived from project list
  const metrics = useMemo(() => {
    const active = projects.filter(p => ['andamento', 'pendente', 'atrasado'].includes(p.status)).length;
    const done = projects.filter(p => p.status === 'concluido').length;
    const overdue = projects.filter(p => p.status === 'atrasado').length;
    const pending = projects.filter(p => p.status === 'pendente').length + overdue;
    return {
      active,
      done,
      pending,
      overdue
    };
  }, [projects]);
  const toggleTask = (projectId, taskId) => {
    setProjects(prev => prev.map(p => p.id !== projectId ? p : {
      ...p,
      tasks: p.tasks.map(t => t.id !== taskId ? t : {
        ...t,
        done: !t.done
      }),
      updated: 'agora'
    }));
    setSavedFlash(true);
    setTimeout(() => setSavedFlash(false), 2400);
  };
  const createProject = ({
    name,
    subtitle,
    notes
  }) => {
    const id = 'p' + (projects.length + 1) + '-' + Date.now();
    setProjects(prev => [{
      id,
      name,
      subtitle,
      status: 'rascunho',
      updated: 'agora',
      notes: notes || 'Sem notas ainda.',
      tasks: []
    }, ...prev]);
    setShowNew(false);
  };
  const monthLabel = `${MONTHS[monthIdx]} ${year}`;
  const stepMonth = dir => {
    let m = monthIdx + dir,
      y = year;
    if (m < 0) {
      m = 11;
      y -= 1;
    }
    if (m > 11) {
      m = 0;
      y += 1;
    }
    setMonthIdx(m);
    setYear(y);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      minHeight: '100vh',
      background: 'var(--cor-fundo)',
      fontFamily: 'var(--fonte-ui)',
      color: 'var(--cor-texto)'
    }
  }, /*#__PURE__*/React.createElement(Sidebar, {
    section: section,
    onSection: setSection,
    projectCount: projects.length
  }), /*#__PURE__*/React.createElement("main", {
    style: {
      flex: 1,
      minWidth: 0,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement(TopBar, {
    eyebrow: section === 'resumo' ? 'Painel' : 'Lista',
    title: section === 'resumo' ? 'Resumo do mês' : section === 'projetos' ? 'Todos os projetos' : 'Notas',
    onNew: () => setShowNew(true),
    month: monthLabel,
    onPrevMonth: () => stepMonth(-1),
    onNextMonth: () => stepMonth(+1)
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      padding: '32px 48px 64px',
      display: 'flex',
      flexDirection: 'column',
      gap: 32
    }
  }, savedFlash && /*#__PURE__*/React.createElement(Alert, {
    variant: "info"
  }, "Seus dados foram salvos automaticamente."), section === 'resumo' && /*#__PURE__*/React.createElement(ResumoView, {
    projects: projects,
    metrics: metrics,
    onOpenProject: setOpenProjectId,
    onSeeAll: () => setSection('projetos')
  }), section === 'projetos' && /*#__PURE__*/React.createElement(ProjetosView, {
    projects: projects,
    onOpenProject: setOpenProjectId
  }), section === 'notas' && /*#__PURE__*/React.createElement(NotasView, null))), /*#__PURE__*/React.createElement(ProjectDetail, {
    project: openProject,
    onClose: () => setOpenProjectId(null),
    onToggleTask: toggleTask
  }), /*#__PURE__*/React.createElement(NewProjectModal, {
    open: showNew,
    onClose: () => setShowNew(false),
    onCreate: createProject
  }));
}
function ResumoView({
  projects,
  metrics,
  onOpenProject,
  onSeeAll
}) {
  const active = projects.filter(p => ['andamento', 'pendente', 'atrasado'].includes(p.status)).slice(0, 4);
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("section", {
    style: {
      display: 'grid',
      gridTemplateColumns: 'repeat(3, 1fr)',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement(MetricCard, {
    label: "Projetos ativos",
    value: metrics.active,
    delta: "+2 no m\xEAs",
    deltaTone: "up"
  }), /*#__PURE__*/React.createElement(MetricCard, {
    label: "Conclu\xEDdos",
    value: metrics.done,
    delta: "+1 no m\xEAs",
    deltaTone: "up"
  }), /*#__PURE__*/React.createElement(MetricCard, {
    label: "Pend\xEAncias",
    value: metrics.pending,
    delta: metrics.overdue ? `${metrics.overdue} atrasada${metrics.overdue > 1 ? 's' : ''}` : 'em dia',
    deltaTone: metrics.overdue ? 'warn' : 'flat'
  })), /*#__PURE__*/React.createElement("section", null, /*#__PURE__*/React.createElement(BarChart, {
    title: "Atividade por semana",
    data: [{
      label: 'sem 1',
      value: 11,
      color: 'var(--dados-1)'
    }, {
      label: 'sem 2',
      value: 16,
      color: 'var(--dados-2)'
    }, {
      label: 'sem 3',
      value: 8,
      color: 'var(--dados-3)'
    }, {
      label: 'sem 4',
      value: 19,
      color: 'var(--dados-4)'
    }, {
      label: 'sem 5',
      value: 13,
      color: 'var(--dados-5)'
    }]
  })), /*#__PURE__*/React.createElement("section", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Em movimento"), /*#__PURE__*/React.createElement("h2", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 18,
      fontWeight: 600,
      color: 'var(--cor-texto-forte)',
      margin: 0,
      whiteSpace: 'nowrap'
    }
  }, "Projetos ativos")), /*#__PURE__*/React.createElement(Button, {
    variant: "texto",
    onClick: onSeeAll
  }, "Ver todos")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, active.map(p => /*#__PURE__*/React.createElement(ProjectRow, {
    key: p.id,
    project: p,
    onClick: () => onOpenProject(p.id)
  })))));
}
function ProjetosView({
  projects,
  onOpenProject
}) {
  return /*#__PURE__*/React.createElement("section", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, projects.map(p => /*#__PURE__*/React.createElement(ProjectRow, {
    key: p.id,
    project: p,
    onClick: () => onOpenProject(p.id)
  })));
}
function NotasView() {
  return /*#__PURE__*/React.createElement("section", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 12,
      maxWidth: '60ch'
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Em branco"), /*#__PURE__*/React.createElement("h2", {
    style: {
      fontFamily: 'var(--fonte-display)',
      fontSize: 28,
      fontWeight: 600,
      color: 'var(--cor-texto-forte)',
      margin: 0
    }
  }, "Ainda n\xE3o h\xE1 notas soltas."), /*#__PURE__*/React.createElement("p", {
    style: {
      color: 'var(--cor-texto-suave)',
      fontSize: 15,
      lineHeight: 1.55,
      margin: 0
    }
  }, "Notas avulsas v\xE3o aparecer aqui \u2014 para anotar uma ideia sem ter que criar um projeto inteiro. Ainda n\xE3o constru\xEDdo, e tudo bem: a gente adiciona quando um projeto real precisar."));
}
window.App = App;
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/casa-projetos/App.jsx", error: String((e && e.message) || e) }); }

// ui_kits/casa-projetos/MetricCard.jsx
try { (() => {
/* global React */

function MetricCard({
  label,
  value,
  delta,
  deltaTone = 'flat'
}) {
  const deltaColor = {
    up: 'var(--cor-sucesso)',
    warn: 'var(--cor-atencao)',
    down: 'var(--cor-erro)',
    flat: 'var(--cor-texto-suave)'
  }[deltaTone];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: 'var(--cor-superficie)',
      border: '1px solid var(--cor-borda)',
      borderRadius: 10,
      padding: 20,
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      textTransform: 'uppercase',
      letterSpacing: '0.06em',
      color: 'var(--cor-texto-suave)'
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--fonte-display)',
      fontSize: 36,
      fontWeight: 600,
      lineHeight: 1.05,
      color: 'var(--cor-texto-forte)',
      marginTop: 6,
      fontVariantNumeric: 'tabular-nums'
    }
  }, value), delta && /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      color: deltaColor,
      marginTop: 4
    }
  }, delta));
}
function BarChart({
  title,
  data
}) {
  // data: [{ label, value, color }]
  const max = Math.max(...data.map(d => d.value));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: 'var(--cor-superficie)',
      border: '1px solid var(--cor-borda)',
      borderRadius: 10,
      padding: 20,
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11,
      fontWeight: 600,
      textTransform: 'uppercase',
      letterSpacing: '0.06em',
      color: 'var(--cor-texto-suave)'
    }
  }, title), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'flex-end',
      gap: 12,
      height: 160
    }
  }, data.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: 1,
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: 8,
      height: '100%',
      justifyContent: 'flex-end'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: '100%',
      height: `${d.value / max * 130}px`,
      minHeight: 4,
      background: d.color,
      borderRadius: '4px 4px 0 0'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, d.label)))));
}
Object.assign(window, {
  MetricCard,
  BarChart
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/casa-projetos/MetricCard.jsx", error: String((e && e.message) || e) }); }

// ui_kits/casa-projetos/NewProjectModal.jsx
try { (() => {
/* global React, Button, Field, Eyebrow */
const {
  useState,
  useEffect
} = React;
function NewProjectModal({
  open,
  onClose,
  onCreate
}) {
  const [name, setName] = useState('');
  const [subtitle, setSubtitle] = useState('');
  const [notes, setNotes] = useState('');
  const [error, setError] = useState('');
  useEffect(() => {
    if (open) {
      setName('');
      setSubtitle('');
      setNotes('');
      setError('');
    }
  }, [open]);
  useEffect(() => {
    if (!open) return;
    const handler = e => {
      if (e.key === 'Escape') onClose();
    };
    window.addEventListener('keydown', handler);
    return () => window.removeEventListener('keydown', handler);
  }, [open, onClose]);
  if (!open) return null;
  const submit = e => {
    e.preventDefault();
    if (!name.trim()) {
      setError('Dê um nome para o projeto.');
      return;
    }
    onCreate({
      name: name.trim(),
      subtitle: subtitle.trim() || 'Sem subtítulo',
      notes: notes.trim()
    });
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    onClick: onClose,
    style: {
      position: 'fixed',
      inset: 0,
      background: 'rgba(34,31,26,0.35)',
      zIndex: 70
    }
  }), /*#__PURE__*/React.createElement("form", {
    onSubmit: submit,
    style: {
      position: 'fixed',
      top: '50%',
      left: '50%',
      transform: 'translate(-50%, -50%)',
      width: 480,
      zIndex: 80,
      background: 'var(--cor-camada)',
      border: '1px solid var(--cor-borda-camada)',
      borderRadius: 'var(--raio-grande)',
      boxShadow: 'var(--sombra-grande)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '24px 28px 8px',
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, {
    color: "var(--cor-destaque-texto)"
  }, "Criar"), /*#__PURE__*/React.createElement("h2", {
    style: {
      fontFamily: 'var(--fonte-display)',
      fontSize: 24,
      fontWeight: 600,
      color: 'var(--cor-texto-forte)',
      margin: 0
    }
  }, "Novo projeto"), /*#__PURE__*/React.createElement("p", {
    style: {
      fontSize: 13,
      color: 'var(--cor-texto-suave)',
      margin: 0
    }
  }, "Aparece na lista da p\xE1gina inicial.")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 28px',
      display: 'flex',
      flexDirection: 'column',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Nome do projeto",
    value: name,
    onChange: v => {
      setName(v);
      if (error) setError('');
    },
    placeholder: "Ex.: Reforma da cozinha",
    error: error
  }), /*#__PURE__*/React.createElement(Field, {
    label: "Subt\xEDtulo",
    value: subtitle,
    onChange: setSubtitle,
    placeholder: "Ex.: arm\xE1rios, bancada e el\xE9trica",
    hint: "Uma linha curta para lembrar do escopo."
  }), /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-texto)'
    }
  }, "Notas iniciais"), /*#__PURE__*/React.createElement("textarea", {
    value: notes,
    onChange: e => setNotes(e.target.value),
    rows: 3,
    placeholder: "Comece pelo que j\xE1 est\xE1 claro \u2014 o resto vem depois.",
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      color: 'var(--cor-texto)',
      padding: '9px 12px',
      resize: 'vertical',
      border: '1px solid var(--cor-borda-forte)',
      borderRadius: 6,
      background: 'var(--cor-superficie)',
      outline: 'none'
    },
    onFocus: e => {
      e.currentTarget.style.borderColor = 'var(--cor-acao)';
      e.currentTarget.style.boxShadow = 'var(--anel-foco)';
    },
    onBlur: e => {
      e.currentTarget.style.borderColor = 'var(--cor-borda-forte)';
      e.currentTarget.style.boxShadow = 'none';
    }
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 28px 24px',
      display: 'flex',
      justifyContent: 'flex-end',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "texto",
    onClick: onClose
  }, "Cancelar"), /*#__PURE__*/React.createElement(Button, {
    variant: "primario",
    type: "submit"
  }, "Criar projeto"))));
}
Object.assign(window, {
  NewProjectModal
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/casa-projetos/NewProjectModal.jsx", error: String((e && e.message) || e) }); }

// ui_kits/casa-projetos/ProjectDetail.jsx
try { (() => {
/* global React, Badge, Button, Eyebrow */
const {
  useEffect
} = React;
function ProjectDetail({
  project,
  onClose,
  onToggleTask
}) {
  // Esc to close
  useEffect(() => {
    if (!project) return;
    const handler = e => {
      if (e.key === 'Escape') onClose();
    };
    window.addEventListener('keydown', handler);
    return () => window.removeEventListener('keydown', handler);
  }, [project, onClose]);
  if (!project) return null;
  const variantFor = {
    rascunho: 'neutro',
    andamento: 'info',
    pendente: 'atencao',
    atrasado: 'erro',
    concluido: 'sucesso'
  }[project.status] || 'neutro';
  const statusLabel = {
    rascunho: 'Rascunho',
    andamento: 'Em andamento',
    pendente: 'Pendente',
    atrasado: 'Atrasado',
    concluido: 'Concluído'
  }[project.status] || project.status;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    onClick: onClose,
    style: {
      position: 'fixed',
      inset: 0,
      background: 'rgba(34,31,26,0.25)',
      zIndex: 50
    }
  }), /*#__PURE__*/React.createElement("aside", {
    style: {
      position: 'fixed',
      right: 0,
      top: 0,
      bottom: 0,
      width: 480,
      background: 'var(--cor-camada)',
      borderLeft: '1px solid var(--cor-borda-camada)',
      boxShadow: 'var(--sombra-grande)',
      backdropFilter: 'var(--camada-filtro)',
      WebkitBackdropFilter: 'var(--camada-filtro)',
      zIndex: 60,
      display: 'flex',
      flexDirection: 'column'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '24px 32px',
      borderBottom: '1px solid var(--cor-borda)',
      display: 'flex',
      flexDirection: 'column',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-start',
      gap: 16
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, {
    color: "var(--cor-destaque-texto)"
  }, "Projeto"), /*#__PURE__*/React.createElement("button", {
    onClick: onClose,
    style: {
      all: 'unset',
      cursor: 'pointer',
      width: 28,
      height: 28,
      borderRadius: 8,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: 'var(--cor-texto-suave)'
    },
    onMouseEnter: e => e.currentTarget.style.background = 'var(--cor-superficie-2)',
    onMouseLeave: e => e.currentTarget.style.background = 'transparent'
  }, /*#__PURE__*/React.createElement("svg", {
    width: "16",
    height: "16",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("line", {
    x1: "18",
    y1: "6",
    x2: "6",
    y2: "18"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "6",
    y1: "6",
    x2: "18",
    y2: "18"
  })))), /*#__PURE__*/React.createElement("h2", {
    style: {
      fontFamily: 'var(--fonte-display)',
      fontSize: 28,
      fontWeight: 600,
      color: 'var(--cor-texto-forte)',
      margin: 0,
      lineHeight: 1.15
    }
  }, project.name), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      marginTop: 4
    }
  }, /*#__PURE__*/React.createElement(Badge, {
    variant: variantFor
  }, statusLabel), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--cor-texto-suave)'
    }
  }, "Atualizado ", project.updated))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: 'auto',
      padding: '24px 32px',
      display: 'flex',
      flexDirection: 'column',
      gap: 24
    }
  }, /*#__PURE__*/React.createElement("section", null, /*#__PURE__*/React.createElement(Eyebrow, {
    style: {
      marginBottom: 8
    }
  }, "Notas"), /*#__PURE__*/React.createElement("p", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      lineHeight: 1.55,
      color: 'var(--cor-texto)',
      margin: 0,
      maxWidth: '60ch'
    }
  }, project.notes)), /*#__PURE__*/React.createElement("section", null, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement(Eyebrow, null, "Tarefas"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, project.tasks.filter(t => t.done).length, " de ", project.tasks.length, " conclu\xEDdas")), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 4
    }
  }, project.tasks.map(task => /*#__PURE__*/React.createElement(TaskRow, {
    key: task.id,
    task: task,
    onToggle: () => onToggleTask(project.id, task.id)
  }))))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: '16px 32px',
      borderTop: '1px solid var(--cor-borda)',
      display: 'flex',
      justifyContent: 'flex-end',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(Button, {
    variant: "texto",
    onClick: onClose
  }, "Fechar"), /*#__PURE__*/React.createElement(Button, {
    variant: "secundario"
  }, "Editar projeto"))));
}
function TaskRow({
  task,
  onToggle
}) {
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      padding: '10px 12px',
      borderRadius: 8,
      cursor: 'pointer',
      transition: 'background 0.15s ease'
    },
    onMouseEnter: e => e.currentTarget.style.background = 'var(--cor-fundo)',
    onMouseLeave: e => e.currentTarget.style.background = 'transparent'
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 18,
      height: 18,
      flex: 'none',
      borderRadius: 6,
      border: `1.5px solid ${task.done ? 'var(--cor-acao)' : 'var(--cor-borda-forte)'}`,
      background: task.done ? 'var(--cor-acao)' : 'transparent',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: 'var(--cor-texto-invertido)',
      transition: 'all 0.15s ease'
    }
  }, task.done && /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "3",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, /*#__PURE__*/React.createElement("polyline", {
    points: "20 6 9 17 4 12"
  }))), /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: task.done,
    onChange: onToggle,
    style: {
      display: 'none'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      color: task.done ? 'var(--cor-texto-suave)' : 'var(--cor-texto)',
      textDecoration: task.done ? 'line-through' : 'none',
      flex: 1
    }
  }, task.label));
}
Object.assign(window, {
  ProjectDetail
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/casa-projetos/ProjectDetail.jsx", error: String((e && e.message) || e) }); }

// ui_kits/casa-projetos/ProjectRow.jsx
try { (() => {
/* global React, Badge */

function ProjectRow({
  project,
  onClick
}) {
  const variantFor = {
    rascunho: 'neutro',
    andamento: 'info',
    pendente: 'atencao',
    atrasado: 'erro',
    concluido: 'sucesso'
  }[project.status] || 'neutro';
  const statusLabel = {
    rascunho: 'Rascunho',
    andamento: 'Em andamento',
    pendente: 'Pendente',
    atrasado: 'Atrasado',
    concluido: 'Concluído'
  }[project.status] || project.status;
  const total = project.tasks.length;
  const done = project.tasks.filter(t => t.done).length;
  const pct = total === 0 ? 0 : done / total * 100;
  return /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    style: {
      all: 'unset',
      cursor: 'pointer',
      display: 'grid',
      gridTemplateColumns: '1fr auto 180px 100px',
      gap: 24,
      alignItems: 'center',
      padding: '16px 20px',
      background: 'var(--cor-superficie)',
      border: '1px solid var(--cor-borda)',
      borderRadius: 10,
      transition: 'background 0.15s ease, border-color 0.15s ease'
    },
    onMouseEnter: e => {
      e.currentTarget.style.background = 'var(--cor-fundo)';
      e.currentTarget.style.borderColor = 'var(--cor-borda-forte)';
    },
    onMouseLeave: e => {
      e.currentTarget.style.background = 'var(--cor-superficie)';
      e.currentTarget.style.borderColor = 'var(--cor-borda)';
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      fontWeight: 600,
      color: 'var(--cor-texto-forte)'
    }
  }, project.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--cor-texto-suave)'
    }
  }, project.subtitle)), /*#__PURE__*/React.createElement(Badge, {
    variant: variantFor
  }, statusLabel), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'baseline',
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)',
      textTransform: 'uppercase',
      letterSpacing: '0.06em',
      fontWeight: 600
    }
  }, "Tarefas"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)',
      fontVariantNumeric: 'tabular-nums'
    }
  }, done, "/", total)), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 6,
      background: 'var(--cor-superficie-2)',
      borderRadius: 999,
      overflow: 'hidden'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: `${pct}%`,
      height: '100%',
      background: 'var(--cor-acao)',
      transition: 'width 0.3s ease'
    }
  }))), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--cor-texto-suave)',
      textAlign: 'right',
      fontVariantNumeric: 'tabular-nums'
    }
  }, project.updated));
}
Object.assign(window, {
  ProjectRow
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/casa-projetos/ProjectRow.jsx", error: String((e && e.message) || e) }); }

// ui_kits/casa-projetos/Sidebar.jsx
try { (() => {
/* global React */

const AA_MONOGRAM = /*#__PURE__*/React.createElement("svg", {
  viewBox: "0 0 496 496",
  "aria-hidden": "true",
  style: {
    width: '100%',
    height: '100%'
  }
}, /*#__PURE__*/React.createElement("g", {
  transform: "translate(0,496) scale(0.1,-0.1)",
  fill: "currentColor",
  stroke: "none"
}, /*#__PURE__*/React.createElement("path", {
  d: "M2309 4956 c-2 -2 -39 -6 -82 -10 -274 -23 -579 -112 -850 -246 -484 -241 -876 -633 -1115 -1115 -133 -266 -194 -470 -247 -820 -14 -94 -14 -433 0 -556 67 -582 321 -1101 736 -1504 385 -374 837 -597 1379 -682 179 -27 540 -25 727 5 745 120 1390 559 1767 1204 152 261 258 560 308 871 30 187 32 548 5 727 -85 542 -308 994 -682 1379 -472 486 -1098 749 -1786 750 -86 0 -158 -1 -160 -3z m454 -252 c403 -49 792 -213 1122 -474 44 -35 133 -117 198 -182 l118 -120 -24 -41 c-107 -185 -752 -1289 -773 -1324 l-14 -23 -395 0 -395 0 0 855 0 856 -122 -3 -122 -3 -860 -1476 c-474 -811 -868 -1486 -877 -1499 -16 -24 -16 -24 -73 73 -273 464 -369 1017 -270 1557 79 433 297 843 619 1165 496 496 1164 725 1868 639z m-403 -1549 l0 -615 -361 0 c-198 0 -359 4 -357 8 7 18 711 1222 715 1222 1 0 3 -277 3 -615z m2050 470 c115 -190 215 -446 264 -675 108 -504 35 -1053 -200 -1500 -395 -750 -1149 -1211 -1983 -1212 -266 0 -482 35 -726 116 -357 119 -706 347 -943 613 l-62 70 53 91 c28 49 210 360 402 691 l350 601 398 0 397 0 0 -855 0 -855 119 0 118 0 641 1098 c352 603 740 1268 862 1477 235 403 248 425 255 425 2 0 27 -38 55 -85z m-1117 -1253 c-278 -485 -689 -1177 -693 -1166 -3 7 -4 283 -2 614 l2 600 360 0 360 0 -27 -48z"
})));
function Sidebar({
  section,
  onSection,
  projectCount
}) {
  const items = [{
    id: 'resumo',
    label: 'Resumo'
  }, {
    id: 'projetos',
    label: 'Projetos',
    count: projectCount
  }, {
    id: 'notas',
    label: 'Notas'
  }];
  return /*#__PURE__*/React.createElement("aside", {
    style: {
      width: 240,
      flex: 'none',
      borderRight: '1px solid var(--cor-borda)',
      background: 'var(--cor-fundo)',
      padding: '32px 20px',
      display: 'flex',
      flexDirection: 'column',
      gap: 32
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 26,
      height: 26,
      color: 'var(--cor-texto-forte)'
    }
  }, AA_MONOGRAM), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 12,
      fontWeight: 600,
      textTransform: 'uppercase',
      letterSpacing: '0.08em',
      color: 'var(--cor-texto-suave)'
    }
  }, "Casa & projetos")), /*#__PURE__*/React.createElement("nav", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 2
    }
  }, items.map(it => {
    const active = it.id === section;
    return /*#__PURE__*/React.createElement("button", {
      key: it.id,
      onClick: () => onSection(it.id),
      style: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        fontFamily: 'var(--fonte-ui)',
        fontSize: 15,
        fontWeight: active ? 600 : 500,
        padding: '8px 12px',
        borderRadius: 10,
        border: 'none',
        cursor: 'pointer',
        background: active ? 'var(--cor-superficie-2)' : 'transparent',
        color: active ? 'var(--cor-texto-forte)' : 'var(--cor-texto)',
        textAlign: 'left',
        transition: 'background 0.15s ease'
      }
    }, /*#__PURE__*/React.createElement("span", null, it.label), it.count != null && /*#__PURE__*/React.createElement("span", {
      style: {
        fontSize: 11,
        fontWeight: 600,
        color: 'var(--cor-texto-suave)',
        fontVariantNumeric: 'tabular-nums'
      }
    }, it.count));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: '1px solid var(--cor-borda)',
      paddingTop: 16,
      display: 'flex',
      alignItems: 'center',
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 32,
      height: 32,
      borderRadius: 999,
      background: 'var(--cor-destaque)',
      color: 'var(--cor-texto-invertido)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      fontFamily: 'var(--fonte-display)',
      fontSize: 14,
      fontWeight: 600
    }
  }, "A"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      lineHeight: 1.2
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-texto)'
    }
  }, "Ana"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)'
    }
  }, "Casa do Bosque"))));
}
Object.assign(window, {
  Sidebar
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/casa-projetos/Sidebar.jsx", error: String((e && e.message) || e) }); }

// ui_kits/casa-projetos/TopBar.jsx
try { (() => {
/* global React, Button, Eyebrow */

function TopBar({
  title,
  eyebrow,
  onNew,
  month,
  onPrevMonth,
  onNextMonth
}) {
  return /*#__PURE__*/React.createElement("header", {
    style: {
      display: 'flex',
      justifyContent: 'space-between',
      alignItems: 'flex-end',
      gap: 24,
      padding: '40px 48px 24px',
      borderBottom: '1px solid var(--cor-borda)'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6,
      minWidth: 0,
      flexShrink: 1
    }
  }, eyebrow && /*#__PURE__*/React.createElement(Eyebrow, {
    color: "var(--cor-destaque-texto)"
  }, eyebrow), /*#__PURE__*/React.createElement("h1", {
    style: {
      fontFamily: 'var(--fonte-display)',
      fontSize: 36,
      fontWeight: 600,
      lineHeight: 1.1,
      letterSpacing: '-0.01em',
      color: 'var(--cor-texto-forte)',
      margin: 0
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 12,
      flexShrink: 0,
      whiteSpace: 'nowrap'
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      alignItems: 'center',
      background: 'var(--cor-superficie)',
      border: '1px solid var(--cor-borda-forte)',
      borderRadius: 10,
      padding: 2
    }
  }, /*#__PURE__*/React.createElement(ChevronButton, {
    onClick: onPrevMonth,
    dir: "left"
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-texto)',
      padding: '0 12px',
      minWidth: 130,
      textAlign: 'center',
      fontVariantNumeric: 'tabular-nums'
    }
  }, month), /*#__PURE__*/React.createElement(ChevronButton, {
    onClick: onNextMonth,
    dir: "right"
  })), /*#__PURE__*/React.createElement(Button, {
    variant: "primario",
    onClick: onNew
  }, "Novo projeto")));
}
function ChevronButton({
  onClick,
  dir
}) {
  return /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    style: {
      width: 26,
      height: 26,
      border: 'none',
      background: 'transparent',
      cursor: 'pointer',
      borderRadius: 8,
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: 'var(--cor-texto-suave)'
    },
    onMouseEnter: e => e.currentTarget.style.background = 'var(--cor-superficie-2)',
    onMouseLeave: e => e.currentTarget.style.background = 'transparent'
  }, /*#__PURE__*/React.createElement("svg", {
    width: "12",
    height: "12",
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2.5",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, dir === 'left' ? /*#__PURE__*/React.createElement("polyline", {
    points: "15 18 9 12 15 6"
  }) : /*#__PURE__*/React.createElement("polyline", {
    points: "9 18 15 12 9 6"
  })));
}
Object.assign(window, {
  TopBar
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/casa-projetos/TopBar.jsx", error: String((e && e.message) || e) }); }

// ui_kits/casa-projetos/primitives.jsx
try { (() => {
/* global React */
const {
  useState
} = React;

// ---------- Eyebrow (.t-rotulo) ----------
function Eyebrow({
  children,
  color,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 12,
      fontWeight: 600,
      letterSpacing: '0.06em',
      textTransform: 'uppercase',
      color: color || 'var(--cor-texto-suave)',
      ...style
    }
  }, children);
}

// ---------- Button ----------
function Button({
  variant = 'primario',
  children,
  onClick,
  type = 'button',
  disabled,
  style
}) {
  const [hover, setHover] = useState(false);
  const [active, setActive] = useState(false);
  const [focus, setFocus] = useState(false);
  const base = {
    fontFamily: 'var(--fonte-ui)',
    fontSize: 15,
    fontWeight: 600,
    padding: '10px 18px',
    borderRadius: 10,
    border: '1px solid transparent',
    cursor: disabled ? 'not-allowed' : 'pointer',
    transition: 'background 0.15s ease, border-color 0.15s ease, box-shadow 0.15s ease, transform 0.12s ease, color 0.15s ease',
    opacity: disabled ? 0.45 : 1,
    whiteSpace: 'nowrap',
    outline: 'none'
  };

  // Default (resting) styles per variant
  const variants = {
    primario: {
      background: 'var(--cor-acao)',
      color: 'var(--cor-texto-invertido)',
      boxShadow: '0 1px 2px rgba(34,31,26,0.10)'
    },
    secundario: {
      background: 'var(--cor-superficie)',
      color: 'var(--cor-texto)',
      borderColor: 'var(--cor-borda-forte)'
    },
    texto: {
      background: 'transparent',
      color: 'var(--cor-acao)'
    }
  };

  // State overlays (applied if not disabled)
  let stateStyle = {};
  if (!disabled) {
    if (active) {
      stateStyle = {
        primario: {
          background: 'var(--cor-acao-ativa)',
          boxShadow: 'inset 0 2px 4px rgba(0,0,0,0.18), 0 1px 0 rgba(34,31,26,0.04)',
          transform: 'translateY(1px)'
        },
        secundario: {
          background: 'var(--cor-superficie-2)',
          borderColor: 'var(--cor-borda-controle-hover)',
          boxShadow: 'inset 0 1px 2px rgba(34,31,26,0.10)'
        },
        texto: {
          background: 'var(--cor-acao-suave-2)',
          color: 'var(--cor-acao-ativa)'
        }
      }[variant];
    } else if (hover) {
      stateStyle = {
        primario: {
          background: 'var(--cor-acao-hover)',
          boxShadow: '0 6px 14px rgba(38,64,90,0.22)',
          transform: 'translateY(-1px)'
        },
        secundario: {
          background: 'var(--cor-superficie-2)',
          borderColor: 'var(--cor-borda-controle-hover)'
        },
        texto: {
          background: 'var(--cor-acao-suave)'
        }
      }[variant];
    }
    if (focus) {
      stateStyle = {
        ...stateStyle,
        ...{
          primario: {
            boxShadow: '0 0 0 2px var(--cor-superficie), 0 0 0 5px var(--cor-acao-ativa), 0 1px 2px rgba(34,31,26,0.10)'
          },
          secundario: {
            borderColor: 'var(--cor-acao)',
            boxShadow: 'var(--anel-foco)'
          },
          texto: {
            boxShadow: 'var(--anel-foco)'
          }
        }[variant]
      };
    }
  }
  return /*#__PURE__*/React.createElement("button", {
    type: type,
    disabled: disabled,
    onClick: onClick,
    onMouseEnter: () => setHover(true),
    onMouseLeave: () => {
      setHover(false);
      setActive(false);
    },
    onMouseDown: () => setActive(true),
    onMouseUp: () => setActive(false),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      ...base,
      ...variants[variant],
      ...stateStyle,
      ...style
    }
  }, children);
}

// ---------- Field ----------
function Field({
  label,
  value,
  onChange,
  placeholder,
  hint,
  error,
  type = 'text'
}) {
  const [focus, setFocus] = useState(false);
  return /*#__PURE__*/React.createElement("label", {
    style: {
      display: 'flex',
      flexDirection: 'column',
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 13,
      fontWeight: 600,
      color: 'var(--cor-texto)'
    }
  }, label), /*#__PURE__*/React.createElement("input", {
    type: type,
    value: value,
    placeholder: placeholder,
    onChange: e => onChange?.(e.target.value),
    onFocus: () => setFocus(true),
    onBlur: () => setFocus(false),
    style: {
      fontFamily: 'var(--fonte-ui)',
      fontSize: 15,
      color: 'var(--cor-texto)',
      padding: '9px 12px',
      border: `1px solid ${error ? 'var(--cor-erro)' : 'var(--cor-borda-forte)'}`,
      borderRadius: 6,
      background: 'var(--cor-superficie)',
      outline: 'none',
      borderColor: focus ? 'var(--cor-acao)' : error ? 'var(--cor-erro)' : 'var(--cor-borda-forte)',
      boxShadow: focus ? 'var(--anel-foco)' : 'none',
      transition: 'border-color 0.15s ease, box-shadow 0.15s ease'
    }
  }), error ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-erro)'
    }
  }, error) : hint ? /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--cor-texto-suave)'
    }
  }, hint) : null);
}

// ---------- Badge ----------
const BADGE_STYLES = {
  neutro: {
    bg: 'var(--cor-superficie-2)',
    fg: 'var(--cor-texto-suave)',
    dot: 'var(--cor-borda-controle)'
  },
  info: {
    bg: 'var(--cor-info-bg)',
    fg: 'var(--cor-info)',
    dot: 'var(--cor-info)'
  },
  sucesso: {
    bg: 'var(--cor-sucesso-bg)',
    fg: 'var(--cor-sucesso)',
    dot: 'var(--cor-sucesso)'
  },
  atencao: {
    bg: 'var(--cor-atencao-bg)',
    fg: 'var(--cor-atencao)',
    dot: 'var(--cor-atencao)'
  },
  erro: {
    bg: 'var(--cor-erro-bg)',
    fg: 'var(--cor-erro)',
    dot: 'var(--cor-erro)'
  }
};
function Badge({
  variant = 'neutro',
  dot = true,
  children
}) {
  const s = BADGE_STYLES[variant];
  return /*#__PURE__*/React.createElement("span", {
    style: {
      display: 'inline-flex',
      alignItems: 'center',
      gap: 6,
      fontFamily: 'var(--fonte-ui)',
      fontSize: 11,
      fontWeight: 600,
      padding: '4px 12px',
      borderRadius: 999,
      background: s.bg,
      color: s.fg,
      whiteSpace: 'nowrap'
    }
  }, dot && /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: s.dot
    }
  }), children);
}

// ---------- Alert ----------
function Alert({
  variant = 'info',
  children
}) {
  const s = BADGE_STYLES[variant === 'sucesso' ? 'sucesso' : variant === 'erro' ? 'erro' : variant === 'atencao' ? 'atencao' : 'info'];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: 'flex',
      gap: 12,
      padding: '12px 16px',
      borderRadius: 10,
      fontSize: 13,
      alignItems: 'flex-start',
      background: s.bg,
      color: s.fg,
      fontFamily: 'var(--fonte-ui)'
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: s.dot,
      marginTop: 6,
      flex: 'none'
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      lineHeight: 1.5
    }
  }, children));
}

// ---------- Card ----------
function Card({
  children,
  style
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      background: 'var(--cor-superficie)',
      border: '1px solid var(--cor-borda)',
      borderRadius: 16,
      padding: 24,
      ...style
    }
  }, children);
}
Object.assign(window, {
  Eyebrow,
  Button,
  Field,
  Badge,
  Alert,
  Card
});
})(); } catch (e) { __ds_ns.__errors.push({ path: "ui_kits/casa-projetos/primitives.jsx", error: String((e && e.message) || e) }); }

__ds_ns.Avatar = __ds_scope.Avatar;

__ds_ns.Botao = __ds_scope.Botao;

__ds_ns.Dica = __ds_scope.Dica;

__ds_ns.Etiqueta = __ds_scope.Etiqueta;

__ds_ns.Esqueleto = __ds_scope.Esqueleto;

__ds_ns.Metrica = __ds_scope.Metrica;

__ds_ns.Paginacao = __ds_scope.Paginacao;

__ds_ns.Progresso = __ds_scope.Progresso;

__ds_ns.Tabela = __ds_scope.Tabela;

__ds_ns.Vazio = __ds_scope.Vazio;

__ds_ns.Abas = __ds_scope.Abas;

__ds_ns.Cartao = __ds_scope.Cartao;

__ds_ns.Divisor = __ds_scope.Divisor;

__ds_ns.Gaveta = __ds_scope.Gaveta;

__ds_ns.Migalhas = __ds_scope.Migalhas;

__ds_ns.Alerta = __ds_scope.Alerta;

__ds_ns.Aviso = __ds_scope.Aviso;

__ds_ns.MenuSuspenso = __ds_scope.MenuSuspenso;

__ds_ns.Modal = __ds_scope.Modal;

__ds_ns.PilhaAvisos = __ds_scope.PilhaAvisos;

__ds_ns.Avisos = __ds_scope.Avisos;

__ds_ns.AreaTexto = __ds_scope.AreaTexto;

__ds_ns.Busca = __ds_scope.Busca;

__ds_ns.Calendario = __ds_scope.Calendario;

__ds_ns.Campo = __ds_scope.Campo;

__ds_ns.Escolha = __ds_scope.Escolha;

__ds_ns.Interruptor = __ds_scope.Interruptor;

__ds_ns.MultiSelecao = __ds_scope.MultiSelecao;

__ds_ns.Selecao = __ds_scope.Selecao;

__ds_ns.ATALHOS = __ds_scope.ATALHOS;

__ds_ns.SeletorPeriodo = __ds_scope.SeletorPeriodo;

__ds_ns.Periodo = __ds_scope.Periodo;

__ds_ns.GraficoBarras = __ds_scope.GraficoBarras;

__ds_ns.GraficoDispersao = __ds_scope.GraficoDispersao;

__ds_ns.GraficoFunil = __ds_scope.GraficoFunil;

__ds_ns.GraficoLinhas = __ds_scope.GraficoLinhas;

__ds_ns.GraficoMedidor = __ds_scope.GraficoMedidor;

__ds_ns.GraficoRosca = __ds_scope.GraficoRosca;

__ds_ns.MapaDeCalor = __ds_scope.MapaDeCalor;

__ds_ns.Minigrafico = __ds_scope.Minigrafico;

__ds_ns.BarraLateral = __ds_scope.BarraLateral;

__ds_ns.BarraSuperior = __ds_scope.BarraSuperior;

})();
